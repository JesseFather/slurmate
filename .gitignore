# ── 依赖与构建产物 ──────────────────────────────────────────────────────────
node_modules/
dist/
out/
*.log

# ── 插件包：**构建产物**，不进 git ──────────────────────────────────────────
# 二进制包进 git 等于代码评审死掉：今天 plugins/code-server/client/index.js 是
# 可以逐行看的。仓库里留**源码**，包由作者或 CI 打出来发到别处。
# （唯一一处例外见 .github/workflows/checks.yml 的「符合性向量」那一步 ——
#   那里的"期望的包字节"是十六进制文本，不是 .splug 文件。）
*.splug

# ── Python 字节码缓存 ───────────────────────────────────────────────────────
# cluster/ 下的守护进程自测运行时会在原地生成，属于机器产物，不进仓库
__pycache__/
*.py[cod]

# ── 本机配置与凭据：绝不提交 ────────────────────────────────────────────────
# 客户端的真实配置在 app.getPath('userData')，这几条是防御性兜底 ——
# 万一有人在源码目录里跑了一次，也不会被顺手提交上去。
config.json
secrets.json
pending-goodbye.json

# ── 本集群的真实部署值：绝不提交 ────────────────────────────────────────────
# local/ 装的是「这个仓库的通用代码」与「你这个集群的真实参数」之间的那一层：
# 真实登录地址、真实 slurmate.conf、真实值 denylist 与 pre-commit 钩子。
# 开源之后这一层必须留在本地 —— 它一旦进仓库，脱敏就白做了。
local/

# ── Claude Code 本机配置（共享配置 .claude/settings.json 仍然可以提交）──────
.claude/settings.local.json

# ── 操作系统与编辑器 ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
