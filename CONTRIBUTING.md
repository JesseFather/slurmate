# 贡献指南

Slurmate 让你在 Slurm 集群上用远程开发环境。核心设计只有一句话：**把网络
ACL 的登记簿从「SSH 会话」改挂到「Slurm 作业」上** —— 于是 SSH 闪断不再丢会话。

仓库分成三部分：

- **集群侧（`cluster/`）** —— root 守护进程 `slurmate-sessiond`、用户 CLI `slurmate`、
  作业模板 `run.sbatch`、部署脚本 `deploy.sh`、配置示例 `slurmate.conf.example`
- **客户端（`client/`）** —— Electron 桌面应用
- **插件（`plugins/`）** —— **独立项目**，基座两端都不依赖它们。契约见
  [plugins/README.md](plugins/README.md)。**一个插件都不装是合法状态**，所以基座里
  不许出现任何插件的名字、也不许出现「至少得有一个插件」的假设。

> **如果你只读一节，读[《检查必须能真的失败》](#检查必须能真的失败)。**
> 这个项目对「静默失败」有近乎偏执的关注 —— 系统报告成功而事情没成，是它要消灭的
> 头号问题。这一节决定一段代码能不能进主干，其余的都是次要的。

---

## 搭开发环境

### 客户端

```bash
cd client
npm ci
```

用 `npm ci` 而不是 `npm install`：前者严格按 `package-lock.json` 安装，装出来的依赖树
和 CI、和发布产物一致。用 `npm install` 会在装的同时顺手更新锁文件，于是「本机测试
通过、CI 失败」这种事就有了发生的余地 —— 而且它不会报错。

`npm ci` 会下载 Electron 二进制（约 100–150 MB）。国内网络慢的话可以先设镜像：

```bash
npm config set ELECTRON_MIRROR https://npmmirror.com/mirrors/electron/
```

> **不要把 `node_modules/` 放在网络共享盘上**（比如 NAS 的 SMB 挂载）。
> 文件多、路径长，Windows 上会非常慢甚至失败。请把仓库克隆到本地磁盘再装。

### 集群侧

**没有依赖要装。** 只需要 `python3` 和 `bash`：

- `cluster/slurmate-sessiond`、`cluster/slurmate`、`cluster/nft-compare.py` 是 Python 3，
  只用标准库
- `cluster/deploy.sh` 是 bash；`cluster/run.sbatch` 是一份**模板**，`deploy.sh`
  把插件的作业侧**编织**进去之后才成品

注意前两个**没有 `.py` 后缀** —— 它们最终要安装成 `/usr/local/sbin/slurmate-sessiond`
和 `/usr/local/bin/slurmate`。`.editorconfig` 里为它们单独写了规则，别以为漏了。

**在开发机上就能跑集群侧的测试，不需要集群、不需要 root、不需要装任何东西。**
测试自带临时状态目录与假的 Slurm 命令，配置输入用随仓库分发的
`slurmate.conf.example`，而不是站点私有的 `/etc/slurmate/slurmate.conf`。

---

## 跑测试

### 客户端

```bash
cd client
node --test
```

> ⚠️ **不要给 `node --test` 加路径参数。**
>
> 写成 `node --test test/` 时：Node 18 把参数当**目录**（扫描起点），
> Node 22 把它当 **glob 模式** —— 解析不到就报 `Cannot find module .../test`。
> 本地 Node 18 永远测不出这个问题，CI 用的 Node 22 每次都会红。
> 这个差异真的让 CI 红过一次。
>
> `package.json` 里的 `test` 脚本因此是 `node --test` 的裸形式。不带参数时靠的是官方
> 默认模式 `**/test/**/*.{cjs,mjs,js}`，18 和 22 都支持，且会自动排除 `node_modules/`。
> 要改这一行，先想清楚 CI 跑的是哪个 Node。

### 集群侧

```bash
python3 cluster/test-sessiond-logic.py
```

覆盖安全关键路径（会话文件的硬校验、身份判定）与 Slurm 交互。

### 部署脚本的体检模式

`deploy.sh` 可以只体检、不做任何改动，改脚本后值得先跑一遍：

```bash
sudo bash cluster/deploy.sh --check      # 体检
sudo bash cluster/deploy.sh --dry-run    # 演练：打印将要执行的命令，不执行
```

---

## 提交信息

**用中文写，并且同时说清楚「改了什么」和「为什么」。**

只写「改了什么」是不够的。这个仓库里大量代码是**针对某个具体故障**写的（例子见
下一节），而那些故障本身不会在代码里留下痕迹。半年后有人想判断「这段防御还能不能
删」，唯一的依据就是你写下的「为什么」。

判断标准很朴素：**一个改动如果没有可观察的后果，它就不该存在，也不该有提交信息。**
另外，不要把「顺手重构」和「修一个 bug」塞进同一个提交 —— 后者可以证伪，前者不能，
混在一起就都不可证伪了。

---

## 检查必须能真的失败

这是本项目对贡献者的硬性要求，不是风格偏好。

> **新增的检查必须能真的失败。**
>
> 一个永远不会变红的检查不是检查，是装饰。它比没有检查更糟 —— 因为它让人以为
> 那个位置上有人在看着。

每个新增的校验、断言、比对、兜底分支，提交前请回答一个问题：

**「什么输入会让它失败？」**

答不上来，就说明它不会失败。以下形式一律不算数：

- 遍历一个恒为空的集合
- 断言一个恒为真的条件
- 用 `|| true`、`2>/dev/null`、空 `catch {}` 把失败吃掉
- 让「失败」和「没做」落进同一个分支，打印同一句话

### 仓库里已有的例子

写新检查之前，先看一眼这三个 —— 它们各自踩过一种不同的坑。

**1. `cluster/deploy.sh` 5.2 —— 外部文件哈希比对。**

它比的是部署前后一组**不属于本系统**的文件（`/etc/port-daemon.conf` 之类）的
sha256。这里连着踩过两个坑：

- **运算符优先级造出的假绿。** 早期写法是
  `[[ -e "$f" ]] && sha256sum "$f" | awk ... || echo MISSING`。
  `&&` 与 `||` 同优先级、左结合，于是 `sha256sum` **自己失败**时也会输出 `MISSING`。
  部署前后两份都是 `MISSING`，`diff` 相等，报告于是打印「哈希全部未变」。
  现在把「取不到哈希」单独输出成 `HASHFAIL`，由调用方 grep 产物文件来判定。

- **「两份相等」有两种截然不同的含义。** 在别人的集群上，这几个文件**本来就不存在**，
  前后都是 `MISSING`，`diff` 照样相等。一个是「真的没变」，另一个是「一个字节都没比」。
  所以现在把「不存在」单独计数，并显式声明它没参与结论：

  ```
  [INFO] 外部文件全部不存在（3 项）—— 本项【未参与校验】，不代表它们未被改动
  ```

  而不是让「没有可比的东西」冒充成「一切正常」。

**2. `client/src/main/hosts.js` —— 探测登录节点时读 SSH banner，而不是只做 TCP connect。**

只连不读的话，任何在这个端口上监听的东西（一个端口转发中间盒、一个
REJECT-then-accept 的防火墙）都会被判成「登录节点可达」。检查确实执行了，也确实
通过了 —— 但它通过的原因，和它想证明的事情毫无关系。

**3. `cluster/test-sessiond-logic.py` —— 测试用的 uid 必须等于运行测试的用户。**

这里曾经写死一个数字，恰好等于当时开发者的 uid：测试能过不是因为逻辑对，而是因为
环境凑巧。换个人跑就全线失败，报的还是一句看不出所以然的 `component_bad_owner`。
**靠环境凑巧才通过的测试，和不通过的测试一样没用** —— 它只是在推迟失败。

### 怎么报告「没参与校验」

校验没跑成、或者没有可比对象时，**必须显式说出来**：既不能静默跳过，也不能算作通过。

「通过」和「未参与校验」是两种不同的结论 —— 前者可以据以决策，后者不能。
上面 `deploy.sh` 那句 `[INFO] ... 未参与校验` 就是照这个写的。

---

## 版本号：两段的分工，以及一条发布纪律

框架版本是 **`major.minor`**（两段），插件的版本号是 `x.y.z`（三段）。两段的**分工**
才是要紧的：

| | 含义 |
|---|---|
| `major`（`x`） | **可以不兼容**的那一档 |
| `minor`（`y`） | **加东西但不破坏兼容**的那一档 |

由此有一条**发布纪律**：

> **同一个大版本内不许出现破坏兼容的改动。出现了，就必须升大版本。**

这条纪律不是礼貌 —— 它是 [`docs/PROTOCOL.md`](docs/PROTOCOL.md)〈三方：谁不低于谁〉
里那句承诺（**同一个大版本内，客户端不低于服务端 ⇒ 保证兼容**）**唯一的保证**。
承诺能不能兑现，取决于这条纪律有没有被守。

另外两条：

- **跨大版本升级时必须在 `CHANGELOG.md` 里给明确说明** —— 写清这一次为什么不兼容
  （或者为什么仍然可能通），以及两端各自要做什么。跨大版本连接时客户端也会**明确
  说明**，而它说的那句是这个文件里那句话的另一半。
- ★ **`0.y` 是内测期，整条版本规则不受约束**（架构还在动，每次更新都可能有重大变动）。
  **`1.0` 是分界线**：到那一天要**刻意**摘掉那条例外，而摘掉它会让一条用例红 ——
  摘除是一次动作，不是手滑。

规则与判据只有一份书面形式：`tools/version-fixtures.json`。三端的用例读的是**同一个
文件**（客户端、集群、打包器各读各判得了的那几段）—— 宁可让某一端"读不到那一段"，
也不要让它"自己抄一份"。

---

## 文档

**索引在 [`docs/README.md`](docs/README.md)**（按"你是谁"分三组：管理员 / 插件作者 /
实现者与维护者）。写到一半不知道该放哪一份时先看它 —— 加一份新文档时也顺手在那里
加一行，否则新文档就只有写过它的人知道。

### 指路按符号名，不写行号

**`文件:行号` 形式的引用一律不要写。** 写「`cluster/slurmate-sessiond` 的
`load_session_file()`」，不写「`cluster/slurmate-sessiond:1373-1431`」。

理由不是洁癖，是 0.5.0 那次剥离留下的教训：插件从基座里搬出去之后，
`cluster/slurmate-sessiond` 整体下移了一千多行、`cluster/run.sbatch` 从 748 行
降到 498 行，文档里**一百多条行号引用几乎全部失效**。

**而失效的行号比没有行号更危险**：没有行号，读者按名字搜；行号指错，读者会翻到
一个**看起来像那么回事**的地方，然后按错误的前提理解代码 —— 这正是本项目一路在
清的那类"系统说了一件它并不知道的事"，只不过这次说话的是文档。

同理：

- 行号也不要写进**源码注释**里（`session.js:1767-1895` 这类），除非它指的是同一个
  文件里几行之内的东西；
- 想引用一个**具体的故障**，引它的**编号与文件**（`docs/KNOWN-ISSUES.md` 的 F13），
  不要引行号 —— 编号是稳定的，行号不是。

---

## 脱敏：不要提交真实拓扑

**这个仓库装着一个集群的全部内网拓扑，它是私有的，也会一直保持私有。**
提交任何真实地址都等于把攻击面发到网线上。请一律使用文档保留地址：

| 用 | 不要用 |
|---|---|
| `192.0.2.x` / `198.51.100.x` / `203.0.113.x` | 任何真实 IPv4 |
| `example.com` / `login.example.com` | 任何真实域名 |
| `node01` … `node04` | 任何真实节点名 |
| `alice` / `bob` / `carol` | 任何真实用户名、账户名 |
| `user@example.com` | 任何真实邮箱 |

分区名 `A6000` / `RTX8000` / `2080TI` 是通用 GPU 型号名，可以直接用。

**示例、测试夹具、注释、截图、从终端粘进 issue 的日志，全都算。** 真实地址往往不是
从配置模板里漏出去的，而是从一段「随手贴的报错」里漏出去的。

---

## 许可证

本项目以 [Apache-2.0](LICENSE) 授权。提交贡献即表示你同意以同一许可证授权。

