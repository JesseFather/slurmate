# 变更日志

本文件记录 Slurmate 的显著变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

★ **版本号不遵循语义化版本。** 这个项目的框架版本是 **`major.minor`，两段**
（`major` 无上限、`minor` ∈ `0..255`、禁止前导零、超上界必须进位），插件的版本号
才是三段（`x.y.z`，见 [docs/PLUGIN-SPEC.md](docs/PLUGIN-SPEC.md) §2.3）。从前的
那一行写着"遵循语义化版本"，而语义化版本要求三段 —— 那句话是错的。
规则与判据见 `tools/version-fixtures.json`。

★ **0.2.0 及更早的标题保持原样**：它们是**当时的字符串**，那时的规定就是三段。
把历史条目改写成两段，等于声称发布过一个从没存在过的版本号。

★★ **两段各有分工，而它带来一条发布纪律。**

| | 含义 |
|---|---|
| `major`（`x`） | **可以不兼容**的那一档 |
| `minor`（`y`） | **加东西但不破坏兼容**的那一档 |

- **同一个大版本内不许出现破坏兼容的改动。** 出现了，就必须升大版本。
  这条纪律是"同一个大版本内，客户端不低于服务端 ⇒ 保证兼容"那句承诺**唯一的保证**
  （规则见 [docs/PROTOCOL.md](docs/PROTOCOL.md)〈三方：谁不低于谁〉）。
- **跨大版本升级时必须给明确说明**：在下面这一节里写清"这一次为什么不兼容 / 或者
  为什么仍然可能通"，以及两端各自要做什么。
- ★ **`0.y` 是内测期，整条版本规则不受约束**（架构还在动，每次更新都可能有重大变动）。
  **`1.0` 是"不承诺"与"承诺"的分界线**：到那一天要刻意摘掉那条例外。

## [0.7] — 未发布

> 四处版本号（`client/package.json`、`client/package-lock.json`、
> `cluster/slurmate`、`cluster/slurmate-sessiond`）现在都是 **0.7**。
>
> ⚠️ **与 v0.2 → v0.5 那几次一样，这一版两端必须一起升。** 理由与逐条症状写在
> [docs/PROTOCOL.md](docs/PROTOCOL.md) 的〈协议版本与变更〉—— 最短的版本是：
> **`files` 与 `plugin_file` 被删掉了**，而 v0.6 的客户端判"站点支不支持分发"
> 用的正是 `files`。

### Removed — ★★ 一份内容只剩一条投递方式

v0.6 让同一个插件有**两条**送下去的路：`files` + `plugin_file`（一份文件一次 RPC）
与 `package` + `plugin_package`（一次取回整个 `.splug`）。那是**加法过渡**的形状，
而现在两端都已经走包那条路了，所以这一版把前一条**删掉**：

- `plugins` 的每一项**不再有** `files`；
- op **`plugin_file` 没有了**（新守护进程回 `2 unknown_op`）；
- 只属于那条路的三个 kind 跟着走：`3 plugin_file_unknown`、`4 plugin_file_too_large`、
  `9 plugin_file_changed`；
- 守护进程里 `op_plugin_file`、`Sessiond.plugin_index()`（那份启动快照索引表）
  一并删掉；`plugin_file_index()` 改名成 `plugin_payload_index()`，它**只剩一个
  调用方**（`--check-plugins` 报"这一份分发出去是多大"，不走线）。

★ **为什么删掉它，而不是留着**：它不只是多一条代码路径，是多一条**验签绕得过去**
的路。`plugin_file` 发的是**散装字节** —— 客户端拼不出一个能被签名覆盖的构件，
所以那条路上"这份东西是谁做的"是**没有任何答案**的。§5.4 的钉公钥因此在那边
只能一律拒绝（散装字节证明不了签名者），而那本身就是一个信号：**留着一条必须被
特殊对待的路，不如让它不存在。** 删掉之后这个形状关在**结构**里，不再靠一句判断。

★ **一条判据跟着换了**：「本站支不支持分发」从前看"这一项里有没有 `files`"，
现在看顶层**有没有 `limits`**。两个都是稳定的**协议事实**（不是"这次下载失败了"），
但只有后者在这一版之后仍然存在。假后端、界面、`deliveryOf` 与客户端的那条
`missing[].distributed` 都跟着改了判据 —— 而这条判据**换过三次**（最早是
`Array.isArray(p.files)`），三次都是同一个理由，所以它在代码里有一处**唯一**的答案。

★ **代价写在明处**：v0.6 时"这个包用的是我读不懂的格式"（`package.format` 比客户端
认识的 `FORMAT` 新）是可以**回退**的 —— 退回逐份取，装上再说。**现在没有退路了**，
它只能是一条**明确的失败**，而用户能做的事只有升级客户端。格式演进因此从
"加法过渡"变成"跟着协议版本一起走"。这不是疏忽，是删掉第二条路的直接后果，
界面上那句话也跟着改了（从前写的是"这一次它退回了逐份取那条路（能装上就好）"，
而今天一个都没装上 —— 那句话会撒谎）。

### Changed — ★ 客户端那一半：走的还是包，但**没有第二条路了**

（v0.6 里客户端已经优先走 `plugin_package`；这一版删的是它身后那条退路。）

- `site-plugins.js`：`fetchFiles` 整个删掉，`fetchPackage` 成了唯一一条取内容的
  路；`sameFileSet` 与"逐份清单和整包说的不是同一份东西 ⇒ 拒绝"那条**交叉判据**
  跟着走了 —— 它防的是"两个来源互相矛盾"，而**矛盾需要两个来源**。剩下的是
  "自述 vs 事实"：`fetchPackage` 里那三条（自己数的字节数、自己算的内容摘要、
  响应自己报的那两个数）一条没少。
- ★ **对账里"本机已经有一份"那一步的判据也变了。** v0.6 先看站点这一轮报的
  `files`、本机没有包时才退回本地那个包；现在只剩**本机那个包**。这一改是对的，
  而且不只是"因为别的路没了"：**拿站点这一轮的自述去核本机磁盘上那一份，是让
  对面说的当"本机有的"的判据** —— 而本机那个包是上一次真下载到、逐字节校过的。
- ★ **包单独不在了，仍然不算撤回**（§5.3）：能加载的是树。但那时没有"另一份"
  可比了，于是 `verifyStaged` 接受 `declared = null`（跳过双向比对），只核它自用
  不用得了与自报的身份 —— **而"内容有没有被动过"由台账里那个摘要回答**（摘要是
  从磁盘上算的，动过就变，对不上就会走进待同意）。**少做的那一半要说得出来**：
  `compared: false` 一路传到同意那一屏。
- `backend-fake.js`：假站点默认发的那条路就是包，`debugPackages` / `debugHideFiles`
  两个开关**删掉**（留一个"只能打开唯一那条路"的开关比没有更糟）；`debugBloatPlugin`
  改成往**包里**塞一个装不下的文件 —— 那几条负载上限今天只在客户端**读包**那一步
  执行，所以"站点报了一个装不下的东西"本来就该造在包里。
- ★ **顺带修了一处我自己写错的注释**：客户端的 `HARD_LIMITS.package_bytes` 上写着
  "这个数与守护进程的 `PLUGIN_PACKAGE_MAX_BYTES` 和 CLI 的 `RPC_MAX_RESPONSE_BYTES`
  同一个数" —— **它是错的**，守护进程那个是 2 MiB，另外两处是 4 MiB。真正的关系是
  两条、方向还不同（本站报的 ≤ 客户端那份；包上限 × 4/3 + 信封 ≤ CLI 的读上限）。
  它进得来，是因为"三处一致"读起来像一条**已经有人守着的性质**，而实际守着它们的
  只有 19.11d 的那一条，它比的是另外两个数。

**验证**：客户端 **263 → 262** 条（删掉的那条是"两条投递方式装出来的东西逐字节
相同" —— 只剩一条路之后它没有可比的第二方；其余是就地改写的，见上面〈客户端那一半〉
里那几条 ★）；集群 **485 → 459**（`check(...)` 的调用点 413 → 402）。19.11 那一节整个重写：
它从前测的是逐份取的三条边界 —— 路径白名单、单文件上限、启动后被换过 —— 而那些
断言**连被执行的入口都没有了**，所以它们不是"放松"而是**作废**（前两条今天由客户端
读方执行，第三条由 `plugin_package_changed` 在 19.11c 里原样守着）。补上来的是钉
"删干净了没有"的那几条（`files` 那个键不在、`plugin_file` ⇒ `2 unknown_op`、
`limits` 是新判据）、一条"自检报的份数来自包"，以及下面〈两处是产品里的洞〉第 2 条
那一对；打包器 105 不变。
`bash tools/check-sanitized.sh`（含 `--selftest`）通过；四处版本号都是 **0.7**。
**变异验证 81 处全红、零脚本崩溃、零"找不到要改的那段"。**

### Fixed — 变异验证抓出来的：**一个函数、一条分支、五处方向要翻转**

★ 两处"找不到要改的那段"是**工具的账**，不是产品的账（那两条 find 串指向的是这一版
删掉的代码与改过缩进的代码），脚本跟着改了。下面是**产品**里的两处。

★ 删代码这一类改动有一个特有的假阴性形状：**把一条路删掉之后，原来测它的用例会
"照样绿"** —— 它们测的东西不存在了，而"断言一个不存在的行为"最容易写成永远成立的
一句。所以这一版里每一条被删掉的用例都在**同一次提交里**改成了断言它的**反面**，
而不是直接删掉。81 处变异里有 **5 处的方向因此翻了过来**，每一条都换成了它当初防的
那件事的**近亲**：

| v0.6 的变异 | v0.7 换成 | 为什么不是"删掉那一处" |
|---|---|---|
| 不优先走包（永远走逐份） | **站点没报 `package` 也当成「愿意发」** | 从前守的是"包优先"，现在守的是"不分发 ≠ 分发" |
| 格式太新时**不**回退 | **把「站点太新」与「包坏了」并成一句话** | 没有退路了，要守的变成**那一态本身** |
| 只报 `package`、不报 `files` | **又把 `files` 报回来** | 从前守的是"加法过渡没被掐断"，现在守的是"删掉的字段不许复活" |
| 读包失败时**回退**到逐份取 | **读包失败时静默跳过**（不报失败） | 路没了，但"少报一条失败"与"多一条绕过口"对用户是同一件事 |
| 不做「逐份清单 vs 整包」的交叉判据 | **不拿重算出来的摘要与站点自报的比** | 矛盾需要两个来源；剩下的那半边是"自述 vs 事实" |

另外**两处是产品里的洞**，不是工具的账：

1. ★★ **一个函数，它唯一的调用方没有用例。** `plugin_payload_index()`（"包里有哪些
   文件"的一行包装）在 v0.7 里只剩**一个**调用方 —— `--check` 的那一屏输出，而
   **那一屏没有任何用例**。变异改坏它，一条都不红。**删掉那个函数**，把那一行摊回
   调用点：包能不能读、读出来是什么，`package_read_file()` 已经答完了，不需要另一层。
   ★ 顺带：那个函数的注释写着"它只有一个调用方：`--check-plugins`" —— **写错了**，
   `--check-plugins` 那一屏是自己现读的。这是这一版里第二处"我自己写的注释与代码
   不符"，第一处是 `HARD_LIMITS.package_bytes` 上那句"三处同一个数"。

2. ★★ **一条错误分支，它的用例走不到。** `op_plugin_package` 里"包读不动了"那一句
   要求**快照里还说它在**、而**现读时它没了** —— 而 19.11c 那条用例是先
   `_plugin_cache.clear()` 再删文件，于是快照本身就是"不在"，从**另一个**出口
   （"包在扫描之后不见了"）出去了。两条都回 `9 plugin_package_changed`，说的是
   两件事。补的用例**先问一次 `op_plugins`**（把快照填上）、再删文件 —— 并显式断言
   了那个前提，因为顺序反了的话，新用例会绿着而什么都没测到。

### Removed — 假后端与用例里那些只属于逐份取的东西

- 客户端测试的假站点不再实现 `plugin_file`（回 `2 unknown_op`，真守护进程也是），
  而 `state.fileCalls` **留着并且必须恒为 0** —— 它现在是"客户端有没有偷偷退回
  逐份取"的证据。
- 集群测试 19.11 整节重写：它现在钉的是**删干净了没有**（`files` 那个键不在、
  `plugin_file` 回 `2 unknown_op`），以及新判据（顶层 `limits`）。
- ★ 19.11b 里那条"一次对账的 RPC 次数已经贴上限流阈值"的断言**删了**，理由是它
  **永远绿**：N 是客户端的行为（客户端那边数得清，见 `pkgCalls` / `fileCalls`
  那几条），而它在这里是从一个常量算出来的另一个常量。删掉逐份取之后是
  `1 + 1 + 1 = 3` 次，离桶很远；**要守的是"别把批量传输再引进来"**，而那条由
  "`plugin_file` ⇒ `2 unknown_op`"守着。一条不会红的用例比没有更糟 —— 它会让
  下一个人以为"贴边"这件事有人看着。

### Added — ★★ 版本握手：把一句写在文档里的话，变成代码里的一条判断

`docs/PROTOCOL.md` 一直写着"跨版本的两端连不上，两边必须一起升"，而在这一版之前，
**运行期没有任何一方能读到那个号** —— 客户端从不发自己的版本，守护进程从不问，
客户端**也拿不到**守护进程的版本（`ping` 早就会返回 `version`，但它**从来没有调用
方**）。于是"不兼容"永远只能**事后**发现：v0.6 的客户端撞上 v0.7 的守护进程时，
它把一个更新、完全正常的站点说成「太旧、不支持分发」，界面上一句话都不说地什么
也不做。

现在客户端连上之后问一次 `ping`，拿**双方的版本号**自己判：

| 情形 | 行为 |
|---|---|
| 同 `x` 且客户端 ≥ 服务端 | 放行 —— 这是**被承诺过**的那一格（同 `x` 保证兼容） |
| 同 `x` 且客户端 < 服务端 | **要求升级客户端**（`applyVersionGate`，在任何会话之前） |
| 两个 `x` 不同 | **放行**，但**明确说明**（"不一定，不是绝对不"） |
| `x == 0`（内测期） | 该拦的**降级成说明** —— 架构还在动 |

★ **执行点在客户端，不在守护进程**，因为它只能在客户端：请求信封里没有客户端
版本，老客户端也不会带。所以这条规则的**效力边界**要写在明处（也写进了
`docs/PROTOCOL.md`）：**它第一次执行只能从带它的那一版客户端开始**，「老客户端 ×
新服务端」那一格只有部署纪律能保。

★ **硬停是安全的，因为它在会话建立之前**：连接没成就从来没有 `submit` ⇒ 没有
`session_id` ⇒ 没有心跳 ⇒ 于是也不会经由 `orphan_after` 把用户正在跑的作业
scancel 掉。**判定只在连接时做一次，绝不在会话进行中重新判定。**

★ 用 `ping` 而不是给别的 op 加字段：`ping` 与它的 `version` **自初始提交起就在**，
所以这条规则对**每一个曾经部署过的守护进程**立刻有效 —— 正好对上"客户端要能兼容
老版本服务端"那条要求。**协议线上一个字都没动。**

### Changed — ★ `engines` 的字段级规则：**两侧统一到严格的那一侧**

`engines` 的形状不对时（写成字符串、值是数字、空串、认不得的键、`null`），
守护进程**静默跳过**（当成没有限制），而客户端**拒绝** —— 一侧收下、另一侧拒了，
正是这个仓库点名过的最坏形状。现在统一到**严格的那一侧**。

★ **为什么收紧是安全的**：严格的那一侧**已经在拒**，所以把守护进程也改成拒
**不可能**制造出新的分家，只会让拒绝来得更早（扫描时、安装时）。这是唯一一个
可以证明"收紧不引入新分家"的方向。

★ 连带修掉三处：

- **打包器从前完全不认识 `engines`**（全文 0 次出现），于是它能顺利产出一个两端
  都会拒的包。现在它判**形状**（判不了"满不满足" —— 那个取决于目标站点是哪一版，
  而打包时那个站点还不存在），判据从**共用夹具**来。
- **`packer build` 的版本号检查不再 `String(...)` 强转**：`["1.0.0"]` 经强转会变成
  `"1.0.0"` 于是打包器收下，而客户端与守护进程都拒 —— 又一个"一侧收下、另一侧拒了"。
  与 §2.3「只校验，不归一化」是同一条纪律。
- **`satisfies` 里"读不懂的范围串"与"不满足"被拆开了**：前者是**形状**问题
  （`^0.5` 是规范的拒绝），作者在自己机器上就该看见它，而不是等包装到别人的站点上
  变成一句"本站版本低"。

### Changed — ★ 「站点太新」那一句，**说得更准了**

客户端手里现在有服务端的**真版本号**了，所以"站点比客户端新"不再是一个**推断**：

- 跨大版本 ⇒ 站点是新一代是**对的**，那时格式更新本就在预期之内；
- 其它情形 ⇒ 握手说客户端不低于服务端 ⇒ 服务端**不可能更新** ⇒ 格式比客户端新
  只能是**站点自己不一致**（版本号没升而格式升了，违反了"格式号只因基座版本升
  而升"那条纪律）。★ 此时说"升级客户端"会把用户指去干一件**解决不了问题**的事。

### Added — 那个**睡着的错**：假后端报过一个不合形状的版本号

`client/src/main/backend-fake.js` 的 `ping` 一直返回 `'0.6-demo'`，而它**不匹配**
`FRAMEWORK_VERSION_RE` —— 旁边那句"形状跟着框架版本走（x.y）"的注释写反了。
在没有人读 `ping` 的那些日子里它不可观测；握手一做，它当场就会醒。现在它报真版本，
要造"站点更新 / 跨大版本"改用 `app:debug daemon-version <x.y>`。

### Changed — ★ 两段版本号的分工，以及由此得来的**一条发布纪律**

`major` 是"可以不兼容"那一档、`minor` 是"加东西但不破坏兼容"那一档。所以：

> **同一个大版本内不许出现破坏兼容的改动。出现了，就必须升大版本。**

这条纪律写进了 `CONTRIBUTING.md` 与上面那段头 —— 它是"同 `x` 且客户端不低于
服务端 ⇒ 保证兼容"那句承诺**唯一的保证**。另外：**跨大版本升级时必须给明确说明**，
**`0.y` 是内测期、整条规则不受约束**（`1.0` 那天要刻意摘掉那条例外，而摘掉它会让
一条用例红 —— 摘除是一次动作，不是手滑）。

### Fixed — 一处**我自己在方案里写错的事**

方案里写着"`engines` 不满足的包**装得上、只是不出现在列表里**"。核实之后是**错的**：
安装器走的是同一个 `parse_plugin_manifest`，它返回 `None` 就**一个字节都不写**，
所以装的时候就拒了。真正变的是另一件事（见上面〈Changed〉第一条）：**形状错**的
清单从前在守护进程侧被**静默收下**，于是装得上、也加载得了。

★ 这一条记在这里不是因为它是代码改动，是因为它是一次**断言被核实推翻**——
方案里的那句话如果没人去证伪，它会一路留在文档里，而它是假的。

### Removed — ★★ 插件只剩一条来路：**"用户自己装一份"这条路整个删掉**

客户端的插件以前有**两个根**：站点池（过同意闸）与 `~/.slurmate/plugins/`
（"本机池"，**不过闸**）。第二条是怎么来的：用户可以「从一个包安装…」、也可以
**直接拷一个插件目录进去**，而加载器**扫两层**、两层的目录名都不参与判定 ——
所以拷进去就生效，一个字都不问。

★★ **规范与代码当时是正面相反的，而两边都没有红过。**
`docs/PLUGIN-SPEC.md` §5.1 写着「往池目录里手工放置内容**必须**不产生任何效果」、
§5.2 写着「**禁止**给任何一类插件开免同意的口子（理由：规则一有分支，绕过它的路
就会长出来）」—— 而代码里 `allows` 是
`entry.source !== 'site' || config.isTrusted(…)`，本机池**整类**落在那个 `||`
的左边。**"没有例外"这句话在 `SECURITY.md` 里写着，而它是假的**，因为没有任何一条
用例去验规范那两句。

★ **做法不是给第二个根补一道闸**（那要回答"用户自己刚放进去的东西，让他同意自己
是什么意思"），而是**让那一类插件不存在**：

- `index.js`：根收成一个 `[{ dir: sitePoolDir, source: 'site' }]`；`allows` 去掉
  来源分支，顺带把那个 `!cfg` 的 **fail-open 收成 fail-closed**（它的注释说的是
  "一个插件都加载不了"，而代码是**放行** —— 从前只靠"那次扫描的路径恰好不存在"活着）；
  删五个 IPC（`app:installPlugin` / `uninstallPlugin` / `rescanPlugins` /
  `setDevPlugins` / `openPluginDir`）与 `SOURCE_LABEL` / `sourceLabelOf`；
- `plugins/install.js` **整个文件删掉**；`findPluginDirs` 删第一层（那一层只被
  "手工拷目录"用到）；`loadDir` 删掉；
- `config.js` 删 `devPlugins`（**不迁移**：读到它等于没读到，用例钉着这一条）；
- `backend-fake.js` 删"回落到本机池"那一段 —— ★ 它盖住的正是开发者模式唯一值得演的
  那条路："站点有、本机没有"（兜底让站点报的就是本机有的那些，于是 `missing`
  永远是空的）。

★★ **代价，如实说**：打包版上假站点会**一个插件都不报**（仓库目录不在包里，
`electron-builder.yml` 的 `files` 只有 `src/**`）。那是刻意的降级 —— 别再往回加
兜底，换了名字的兜底还是同一个毛病。账本 **S14**。

★ **`~/.slurmate/plugins/` 里已有的文件不删**（那是用户自己的目录，而 0.2.0 是最后
一个公开版本、它带着本机池）。客户端下一次启动会**说一句它不再被读了** ——
只在那个目录真的有东西时说（`index.js` 的 `warnLegacyPool`）。

★ 服务端那一段（旧布局迁移）当时**没有跟着删** —— 它有次序前提。它随后删掉了，
见下面那一条。

★ **§5.1 与 §5.2 因此从"目标态"变成了"现状"**，各有一条正向用例守着：
往池目录里手放一层树 ⇒ **什么都不发生**（不进列表、不加载、不进「没被加载的」那一栏、**也不报错**）；
只挂一个根，且"没台账 ⇒ 不加载"与"补上台账 ⇒ 加载"两侧都有。

### Removed — ★ 服务端那段旧布局迁移（`.deployed`）删掉了

v0.6 把站点上的插件从"一棵目录树"换成"一插件一个包"时，留了一段**一次性迁移**：
安装器读 `plugins/.deployed` 里记着的**目录名**，逐个确认它确实是插件目录（里面
还有 `plugin.json`）再删掉；`deploy.sh` 的卸载路径里有一份孪生。它当时**专门没有
跟着客户端那次删除一起删**，因为它有**次序前提**而不是排期问题：线上那个实例上
确实躺着旧布局的目录（root 拥有、含客户端代码的一整套副本），跑过一次安装之前
删掉迁移，那批目录就永久留在盘上、且没有任何东西记得它们。

★ **现在删了，因为它等的那个事件不会再发生。** 0.y 不考虑兼容性：这个项目的唯一
操作者**装完直接卸载**，所以"旧布局还留在盘上"没有第二次出现的机会。留着的代价
不是零 —— 一段没人再走的迁移会让人以为那条路还活着，而它的帮助文本还在说
"旧布局迁移在这里"。

★★ **删的是自动化，不是可见性** —— 这两件事在这里极容易混成一件。一个不是包的
目录仍然会被**四条路**点名，一条都没少：

- 守护进程每次启动 `log.error`（`scan_plugins` 的 `problems`，**不是启动错误**）；
- `slurmate-sessiond --check-plugins` 逐条点名；
- `deploy.sh --uninstall`：`plugins/` 里不属于本次部署的东西「一律不删」，并打出
  那句 `rm -rf` 让人自己确认；
- `tools/check-cluster.sh` 直接 `FAIL`。

`scan_plugins` 那条报错也**换了出路**：从前它指回 `deploy.sh`（"跑一次部署就会
被清掉"），现在它说两件事 —— 是源码树就在作者机器上 `packer build` 打成包，
不是就人工删掉。**报出来必须给出下一步**，否则等于把"这怎么办"留给运维去猜。

账本 **S13** 从待办挪进〈已经不再是问题的〉，理由留在那一行 —— 免得下一个人以为
该把迁移加回来。

### Changed — ★★ 「演示模式」没有了，它的位置上是**开发者模式**

★ 原话是「彻底不要演示模式，这个功能没有意义，未配置时一个干净的界面就够了，
这一切都做成真正的开发者模式，并且这是一个图形应用，你做成命令行不合适吧，
Windows 用户去哪里输入命令行」。

判据其实早就有了：**那个东西会真的落盘**（`<userData>/demo-config/`，装进来的插件
退出后还在）—— 那不是"演示"，是一个隔离但持久的沙盒。**它是开发者模式，那就该叫
开发者模式，而且该用界面开关。**

- **删掉**：`--demo` 命令行开关、`package.json` 的 `demo` 脚本、界面上所有「演示
  模式」的说法、`DEMO_FLAG`、`kind === 'demo'`。**未配置时就是一块干净的空界面**，
  不做预览入口，也不会自己进去；
- **新增**：面板最下面一节「开发者模式」，一个复选框 + 一个「立即重启，让它生效」。
  打开之后才露出里面那两块（假站点的插件来源、调试开关）。

★ **两个设置存在 `<userData>/dev-mode.json`，刻意不放进 `config.json`**：那个键要
回答的正是"这次启动读**哪一份**配置"（用户自己那份，还是沙箱那份），放进
`config.json` 就等于"得先知道读哪一份，才知道读哪一份"。顺带还避掉第二个坑 ——
那样一来这个键会在**两份**配置里都出现，而只有一份有读者（先例：`pinned-keys.json`）。

★ **界面上同时有两个值，因为它们不是一件事**：`on`（这次进程在不在开发者模式，
启动时定下）与 `saved`（你在界面上要的是哪一个）。复选框画 `saved`（画 `on` 的话，
点完它自己弹回去，看起来像开关坏了），调试开关那一块画 `on`（假后端没在跑，
那些按钮按下去只会报错），两者不一致时明说「**有改动等着重启**」。两个设置都
**重启后生效** —— 换后端要重建整个客户端，不静默地半生效。

★ **数据隔离保留**（`<userData>/dev-sandbox/`），而**持久化也保留**：它是给插件作者
和改客户端的人用的沙盒，关掉开关不会删掉里面的东西。

★ **插件作者的那条轻路径**：同一节里可以把假站点的插件来源指到**自己那棵插件树**
（默认是仓库里的 `plugins/`）。选完**当场报"读到几个插件"**，一个都读不出来就
**不保存**并说清为什么 —— 否则症状是"重启之后假站点一个插件都不报"，而那句话
指不回原因。

★ **顺带修掉一条与注释相反的兜底**：`createBackend` 从前是「`--demo`，否则只要
`ssh.isImplemented()` 为假就**落到假后端**」，而它旁边那句注释写着"**绝不会**在
真实后端不可用时静默退回"。今天 `isImplemented()` 恒为真，所以那一支是死的 ——
但它描述的是一条这个项目已经不认的行为，所以改成**响亮地坏**。

★ **测试怎么选后端**：从前是 `process.argv.push('--demo')`，现在改成**在 require
之前写那个设置文件** —— 与"用户在界面上勾一下然后重启"是**同一个文件**，不是给
测试开的旁门。新用例三态都有：关着 ⇒ 不走假后端；开着 ⇒ 走；**未配置 ⇒ 不自动进**。
（`boot.test.mjs` 跑"开着"那一半，新文件 `devmode.test.mjs` 跑"关着"那一半 ——
一个进程里 `index.js` 只加载一次，两半不可能共存。）

## [0.6] — 未发布

> 四处版本号（`client/package.json`、`client/package-lock.json`、
> `cluster/slurmate`、`cluster/slurmate-sessiond`）在**这一版**里都是 **0.6**。
>
> ★ **0.6 从未发布过**（写完就直接进了 0.7，见上面那一条）—— 它记在这里是因为
> 它是**加法过渡的那一半**：`files` 与 `package` 两条路同时在，而 0.7 把前者删了。
> 所以下面这些条目的读法是"0.6 那一版加了什么"，不是"有谁装过 0.6"。
>
> ★ **与 v0.2 → v0.5 那三次升级不同，这一版协议上两个方向都能通**（只加字段、
> 只加 op）。

### Added — ★★ 整包那条路：同一个内容，两条投递方式

站点侧的插件早就是一个包了，但**客户端取它仍然是"一份文件一次 RPC"**（两个插件
11 次，正好压在限流桶边上）。这一版把**服务端那一半**接上：`plugins` 的每一项多了
`package`（`{format, bytes, digest}`）、顶层 `limits` 多了 `package_bytes`、
新 op `plugin_package` 一次把**整个包**发回来。
★ **补记**：客户端那一半**已经跟着来了**（见下面〈客户端按包安装〉那一条）——
两条路现在都通，只等 `files` 那条被删掉。

★ **两条路同时在，而且是同一份包**：老客户端只看 `files`，照旧逐份取，一个新字段
都不用认。这是加法过渡的形状 —— 而它**在 0.7 收尾了**（那一条见上）：客户端改走
`package` 之后，`files` 与 `plugin_file` 就删掉了。★ 回头看，**这一段加法过渡没有
任何发布物经历过** —— 0.6 与 0.7 之间没有发布。留着这个记录是因为"当时为什么
两条都留着"与"后来为什么删"是两个都要能回答的问题。

★ **`digest` 是内容摘要（§3.4），不是容器字节的 sha256。** 给同一个负载补一个
签名块，容器字节变了而它**一个字都不变** —— 这正是"摘要不变 ⇒ 还是同一份构件"
（§2.4/§4.2）在协议里的样子。用例**在最底层与走一遍 op 各分辨了一次**，因为这一条
分辨不出来的话，客户端自己算出来的摘要会与本站报的对不上，而**两边都没错**。

★ **整包那条路多给客户端一样东西**：它拿到的是一整个**可以被签名覆盖**的构件，
所以能自己解析、自己重算摘要、自己验签（§4.2/§6.1）。逐份取那条路做不到 ——
那边只有一份一份的字节，拼不出一个能被签名的东西。于是本站报的 `digest` 与本站
转发的字节成了**分开的两件事**，客户端有办法发现它们对不上。

★ **`package: null` 与"没有这个能力"是两件事**（三态纪律）：前者是"这一份现在给不
出来"（包在启动之后不见了，那时 `files` 也是空的），后者看**顶层有没有 `limits`**。
另外两个上限也是**两笔账**：`file_bytes`/`total_bytes`/`max_files` 是负载内的，
`package_bytes` 是链路上的。

★ **那笔假账结清了。** CLI 的读上限从前是 1 MiB，而站点通报的总量上限也是 1 MiB
⇒ "整包一次发"那个数根本读不完。现在上限是 4 MiB，而两者的关系有一条跨文件用例
钉着（19.11d）。症状本来还会被认错：CLI 报"响应太大"，而客户端把它归成
`daemon_unreachable`（code 5）**并退避重试** —— 一次"文件太大"被报成"控制节点上
的守护进程没有响应"。

★ **三处上一阶段留下的漏改**一并修了，它们是同一类"读起来像事实、当时不是"：

1. `cluster/slurmate` 的 `plugins` 空列表分支里还写着"把插件目录放进
   `<prefix>/share/slurmate/plugins/`" —— 那是**源码树布局**时代的说法，包时代
   那句话是错的（放进去的东西会被拒绝，这是刻意的）；
2. `slurmate-sessiond` 里那条注释当时就写着"CLI 读响应的上限是 4 MiB"，而**那时
   它其实是 1 MiB** —— 它写的是目标状态（也就是这一版要做的事），不是当下的状态；
3. `plugins/README.md` 还写着"含链接的插件 `--check-plugins` 会打 ⚠" ——
   而 `plugin_symlinks()` 在那个阶段就删掉了：链接**连包都编不出来**，
   表达不出来的东西谈不上警告。

★ 第 3 条尤其值得记：它是"删掉一个函数"与"删掉一条**说法**"之间的差 —— 上一阶段
删函数时把那句文档留下了，而留下的一句假话比一个多余的空函数更难发现。

★ `--check-plugins` 现在多打一行**完整的内容摘要**（64 位十六进制，不截断）。
它在服务器上是**唯一**能回答"我装上去的这一份是不是作者发布的那一份"的东西 ——
那里没有源码树可对照，能比的只有这个数与作者 `packer inspect` 报的那个。

### Added — ★★ 客户端按包安装：那个"看得见却点不到"的洞，以及删除 = 撤回同意

客户端那一半接上了：一次对账优先走 `plugin_package`（**一条 RPC 取回整个包**），
池里每个版本变成**两样挨着** —— `<id>/<版本>/`（解出来的树，`require()` 用的是它）
与 `<id>/<版本>.splug`（包本身，来路凭证，也是验签的原料）。`files` 那条路一个字
没动，回退只在一个方向上开（见下）。

★ **优先走包不是因为"快"。** 快是顺带；理由是**包才是作者发布的那个构件**：
它是签名的载体（§4.2），它的内容摘要（§3.4）是"是不是同一份东西"的判据，而逐份
取回来的那堆散装字节**谁也证明不了什么**。§5.4 的钉公钥要落地，客户端手里就必须
有容器本身 —— 这一版顺序理清之后，钉子第一次真的接上了。

★ **回退只对"这个包是用一种我看不懂的说法写的"开放**（`package.format` 比认得的
**新**），因为那不是"包坏了"，是格式演进本身，而格式演进必须能加法过渡。**其余
每一条读包的失败都不回退** —— 一个验不过的包加上一条能绕过它的路，等于把验签降级
成一句建议。用例把这两条路**并排**钉着（一条断言它退、一条断言它不退）。

★ **那个洞**（v0.6 就有，这一版修掉）：池里已经有一份、而同意台账对不上时，插件
在注册表里但 `active: false`，于是"插件"那一列滤掉了它、"本站有而本机没有"那一列
又要求注册表里**查不到**它 —— 界面上彻底看不见，**连"点同意"的入口都没有**，
重新同步也救不回来。唯一的痕迹是一个算出来从不渲染的 `inertCount`。
★ 摘要换一次公式，这件事会**一次性对每个用户的每个插件**成立 —— 所以修它不是
美化项，是换公式的前置。现在它走进待同意（带着 `existing: true`），界面上有一块
专门画"没被加载的那些"，并给一个出口（删掉本机那一份）。

★ **§5.3 删除 = 撤回同意**也落地了：对账时发现"台账信任着它、而池里没有它" ⇒
**台账那条当场消失**，于是同一个对账里它自然走进待同意 —— 不需要另写一条规则。
不这么做的话，用户删掉池里那一份之后，下一次对账会按"摘要与台账相符"**静默装
回来、一个字都不问**。文案里也**绝不**断言是谁删的（客户端只知道"不在了"）。

★ **台账条目多了 `alg`**（这个摘要是**哪一个公式**算的），而 `isTrusted` 要求版本
相符。理由很具体：摘要是**一个函数的结果**，函数换了公式之后，把新值与旧值放在
一起比就是一句假话 —— 界面上那句"内容摘要从 X 变成了 Y"会说"内容变了"，而真相是
"我们换了一把尺子"。老的条目（没有 `alg`）按**当时那个公式**解释，所以加这个字段
**不让任何一条旧同意失效**。

★ **开发者模式的「从目录安装…」改成了「从一个包安装…」**（选一个 `.splug` 文件）。
§5.1 说插件进池子只有"安装一个包"这一个动作，而对着一个目录点"安装"是另一件
看起来差不多的事。它把包解开到临时目录，然后**走同一个安装器** —— 池的布局、幂等、
"绝不覆盖内容不同的同一版"那几条只有一份实现。

★ **`HARD_LIMITS` 多了 `package_bytes`（4 MiB）**。它是**另一笔账**：前面四个数说的是
**负载内部**（解出来那些），它说的是**链路上**（整包 base64 之后要装得进一条应答）。
客户端这一份必须**不小于**守护进程与 CLI 那两处（同一个数），否则等于客户端单方面
拒绝一个合规站点发得出来的包。有一条用例钉着客户端**自己**那笔账：一个合法到极点
的包（负载顶到 `total_bytes`）base64 之后必须仍然塞得进 `package_bytes`。

★ 假后端多了一个 `debugPackages`（默认**关**）：开启之后演示站点也发包、也签名，
于是**验签与钉钉子那两条路在演示里走得通**。默认关是刻意的 —— 逐份取那条路是已部署
的 v0.6 站点走的，也是最容易在改动里悄悄烂掉的一条，两条都要有东西在测。
演示站点那把签名钥匙由一个**写死的种子**推出来（不是随机的）：客户端第一次同意时会
钉住它，每次重启换一把钥匙会让钉子在第二次启动时就把演示站点自己的插件拒掉。

★ 池里那个包单独不在了**不构成撤回**（能加载的是树），也**不会被静默取回来** ——
但界面会如实说"（只有解出来的树，没有包）"。瞒着不说的话，用户打开那个目录就会发现
少了一个文件，而界面上一个字都没有。

**验证**：客户端 **232 → 263** 条（`site-plugins.test.mjs` 27 → 48，
`boot.test.mjs` 46 → 51，`config.test.mjs` 52 → 55，`plugin-package.test.mjs`
15 → 17）；集群 485、打包器 105 不变。**变异验证 79 处**（这一版新增 28 处），
全红、零脚本崩溃 —— 其中 5 处第一次报的是"一条都不红"，五处的成因各不相同，
逐条记在下面〈五处假阴性〉那一段里。

### Fixed — 五处**假阴性**：防线在，而用例抓不住它

★ 换公式那段之前说"那个洞必须先修" —— 这一版修了它，而**变异验证立刻证明修得对不对
是另一回事**。79 处变异里有 **5 处一条都不红**，形状各不相同，五处都补了用例或补了
实现：

1. **「不对包里的负载执行那几条上限」一条都不红。** 用例里站点**两条路都报**，于是
   站点那份逐份清单会**先**在 `checkDeclared` 那里被拦下（那一条也在测，但测的是
   **另一个入口**），包那条路上的同一道检查根本没被执行到。修法是把那个用例里的
   `files` 藏起来 —— 让包那条路的检查成为**唯一**的那一道。
   ★ 这是这一批里最值得记的一条：**两条路都对同一件事做检查，于是"两条都测了"是
   假的** —— 走在前面的那条会替后面那条把用例变绿。
2. **「已有那一份、台账对不上时不做钉子判定」一条都不红。** 钉子那件事我只在
   "新下载"那条路上测了。补了一条：池里已经有一份、台账对不上、而钉子对不上 ⇒
   拒绝。"再问一次"**不等于**"可以换个人" —— 不判的话，改一次摘要（或者删一次配置）
   就成了绕开 §5.4 的入口。
3. **「站点愿不愿意发」判据退回"有没有 files" 一条都不红。** 用例是对的，而它断言
   的那份清单是**连接时**取的（`refreshPartitions` 的 `op:plugins`），不是对账那一轮
   —— 开关改完没重新问一次，于是断言的是一个开关改之前就取到的清单。
   ★ 一条**永远绿**的用例：它测的东西确实存在，只是测的是**另一个时刻**的它。
4. **「对"已在池里"那一份点不同意」那条分支一条都不红。** 两个分支恰好都删同一个
   目录（`discardStaged` 收到的就是池里那个路径），所以"删掉了"这个断言分不出它们。
   真正的差别在**台账**：补上一条"埋一条旧摘要的记录，删完它必须没"。不补的话那条
   分支是**装饰**。
5. **「演示站点 `package` 报的摘要换成容器的 sha256」一条都不红** —— 这一处**不是
   用例的问题，是实现的缺口**：客户端当时**根本不看**响应里自报的 `bytes`/`digest`
   （它只核 `op_plugins` 里那一份和 `data`）。★ 那三个数是关于同一份东西的**三个
   说法**，客户端该核所有的 —— 于是补上了核响应那一次的检查（缺席就跳过，三态纪律），
   这一处变异才变红。

### Added — ★★ 集群侧切到包：插件是**一个文件**，而站点会**验签**

站点上的插件从"一棵目录树"变成"一个 `.splug`"（`<prefix>/share/slurmate/plugins/
<ULID>.splug`），并且多了一个**安装器**：`sudo slurmate plugin install <文件…>`。
`deploy.sh` 走同一条路（它只是"把一批包一起装"）。

- **服务器上从头到尾没有源码树。** 包由作者在自己的机器上打（`packer build`），
  管理员下载下来装 —— `deploy.sh` **永不打包**（把它写进文件头了）。
- **安装器做的判断，一条条都会说给人听**（`install_plugins()`）：
  ① 输入必须是普通文件、非符号链接、属主是 root、组/其他不可写；
  ② 包能解析，清单过**守护进程自己那个校验器** —— 所以"装得上但守护进程不认"
     在装的那一刻就红；
  ③ **两个包同一个 id ⇒ 两个都不装**（§6.4）。按 id 命名之后它们的**文件名本身
     就是 id** ⇒ 后装的会静默覆盖先装的，所以这条检查必须在装之前；
  ④ 超过整包上限的**不装**（装了也发不出去：客户端一次取不回那么大的包）。
- ★ **站点也验签了**（规范 §6.4 同步补上）。做法是接**系统 openssl**
  （`pkeyutl -verify -rawin`），不在 Python 里手写 Ed25519。而它回答的是一句
  **更小**的话：*"这个包与它带来的那把钥匙自洽吗"* —— **不是**"这是谁发的"
  （钥匙是随包一起来的，能改包的人也能换钥匙）。"谁发的"靠两侧各自的记忆。
- ★ **站点侧的 §2.5**：安装器按 `id` 记住签名者，此后同一个 id 换了钥匙 ⇒
  **停下来问**，两把指纹都报出来，两条路（换签名者 / 分身）连同后果一起说。
  ★ **反方向同样拦**：记着有签名而这一份没有，同样停下来问 —— 摘掉签名正是
  "降级到不用验"的那一步。越过它的唯一方式是 `--replace-key <记录里那把旧指纹>`
  （取值必须对得上：确认要有依据，不是随手加一个开关）。
- **`--check-plugins` 的机器可读那一段变成四列**
  （`<短名>\t<包路径>\t<ULID>\t<has_job|no_job>`）。第 4 列**必须显式**：
  「没有作业侧」是合法状态，而"取不出来"还可能是包坏了 —— 用一个退出码同时表达
  这两件事，正确的那个分支恰好是**静默**的。deploy.sh 拿它取 `job/start.sh`
  （走 `--extract-package`）去织作业脚本。
- **旧布局的迁移**：安装器按 `.deployed` 标记清掉 v0.6 的 `<名字>/` 目录。
  ★ 不迁的后果不是"多占点地方"：`PLUGINS_DIR` 里会同时躺着旧目录与新包，而守护
  进程只读包 ⇒ **root 拥有的、含客户端代码的一整套副本永久残留且界面上看不见**。
  没被标记记过的目录**一律不删**，但守护进程会把它**报出来**（不静默跳过）。

★ **一处主动删掉的检查**：`plugin_symlinks()` 没有了。它不是"被删掉"，是**表达
不出来** —— 负载里没有"链接"这种条目，所以"这个插件分发不了"这一态不存在了。
留一个永远返回空的函数比没有更糟：它会让人以为那条路还有人守。

★ **协议一个字没改**（`files` / `limits` / `plugin_file` 的字段与语义都没动，
只是那份清单现在从**包里的记录表**算出来）。所以老守护进程 ↔ 新客户端、新守护
进程 ↔ 老客户端两个方向都照常。★ 本仓库自带的那两个插件**还没有包**：它们的 id
铸在打包器存在之前，血统表里没有它们那一条 ⇒ 第一次要 `packer init --adopt`
（一次身份声明），然后提交、`keygen`、`build`。**这一步还没有做**。

### Fixed — ★★ "崩溃伪装成一条都不红"**又回来了**，而这次是从夹具的默认值进来的

变异验证里，把 `needs_job` 改成恒假之后，`cluster/test-sessiond-logic.py` **崩了**
（第 18 节 `sess["cpus"]` 抛 `KeyError`）—— 于是它后面几十条断言一条都不跑，而报告
读起来与"这一处改坏了没人管"一模一样。同一个形状在阶段 2 与阶段 3 各出现过一次，
那两次补的是**调用点**；这一次补的是**判据本身**：

- `run_submit()` 那两个容器改成缺键给 `None` 的 `dict` 子类。**这不是把用例放松**：
  那一节每一条断言的前提都是"上一条成立"，而变异验证时那个前提**就是不成立的** ——
  给 `None` 让它们**红掉**，那才是它们该做的事。
- 默认值也要是那个容器：提交失败时打桩函数根本不会被调用，给一个普通 `{}` 就会在
  下一行崩掉。
- 19.11 里两处裸 `next(...)` 同样会有 `StopIteration`（"清单是空的"正是被测的那件
  事），改成 `next(..., None)`。

★ 记在这里而不是只写进代码，因为它是**这一类**问题而非一处 bug：在这个仓库里，
"夹具假设自己走的是成功路径"每出现一次，就会让一次变异验证看起来通过了。

### Changed — ★ 文档里那些"插件是一个目录"的说法跟着改了

`plugins/README.md`、`docs/ARCHITECTURE.md`、`docs/DEPLOYMENT.md`、
`docs/CONFIGURATION.md`、`docs/TROUBLESHOOTING.md`、`README.md`、`SECURITY.md`、
`tools/check-cluster.sh` 与 `docs/PROTOCOL.md` 一并改到"插件是一个包"。
`docs/DEPLOYMENT.md` 新增〈步骤 0.5：把插件打成包〉—— 因为缺省 `--plugins-src`
指向的是仓库的**源码树**，直接部署会在预检那一步停下来。

### Added — ★ 签名落地：一个 id 一把钥匙、一张血统表、一张钉子表

签名从"格式定了、验签写了"推进到**工具齐了**。四样东西：

- **`packer keygen`**：给这个 id 铸一把 Ed25519 钥匙。公钥写进**血统表**，
  私钥进钥匙库（作者机器上，`0600`，**不进树** —— 一个提交出去的私钥等于血统当场
  作废且收不回来）。
- **`packer sign`**：给一个已经打好的包盖上签名。★ 它**不改内容摘要**（§4.2：签名
  盖的是摘要那 32 个字节，不覆盖信封），所以"给已经打好的包补签名"是合法的 ——
  按 §2.4 它还是同一份构件。两个推论都写进了代码：`build` **不看钥匙库**（它的输出
  只是 `(提交, 选项)` 的函数，§3.5 那句"同输入逐字节相同"因此没有星号）；
  而 Ed25519 是**确定性**签名 ⇒ 同一个包同一把钥匙签两次**逐字节相同**。
- **血统表 `lineage.json`**（§2.5）：在**源码树里**、提交进版本控制 —— clone 要带着
  它走，因为"这个 id 是谁签的"必须**本地可判**。它同时**在负载里**（§3.6：它是一份
  普通文件、不在跳过集里），于是 `sign` 不必知道源码树在哪，而 `verify` / `inspect`
  能发现"表里说归 K1、而签这个包的是 K2"这种陈旧。代价写在明处：给它加一条记录就是
  改内容 ⇒ §2.4 要升版本号（所以正常次序是 `init → keygen → 提交 → build`）。
- **客户端按 `id` 钉公钥**（§5.4）：`config.js` 的 `loadPinnedKeys` /
  `pinPluginKey` 与 `plugin-package.js` 的 `keyVerdict`。上一版那一格是"用一致性
  台账替代"（严格更弱），现在两个判据各就各位。

★ **钉子表是单独一个文件**（`~/.slurmate/pinned-keys.json`），**不放 `config.json`**：
`loadConfig` 只认已知键，于是**旧版本**读一遍再存一遍就会把它抹掉 —— 用户降级一次，
钉子全没了，而那正是 §2.5 的分身判定有了一个重置按钮。

★ **三个刻意的"往拒绝那一侧倒"**，每一个都有一条会红的用例：

| | 规则 | 不这样会怎样 |
|---|---|---|
| 钉过之后收到**不带签名**的包 | 拒绝 | 一份不带签名的构件没法证明是同一个人做的 |
| 钉子表里那一条**读不动**（手改过、字段缺了） | 拒绝 | 退化成"从没钉过" = 静默重新 TOFU 一次 |
| `pinPluginKey` 想把已有的钉子**换成别的一把** | 拒绝 | 分身判定变成一次点击 |

★ 与同意台账**相反**的那一条值得单独说：台账里一条残缺条目丢掉 = 重新问一次
（**安全的那一侧**），所以那里丢掉是对的；钉子丢掉 = 静默重新"首次即信任"
（**不安全的那一侧**），所以这里一条都不许丢。

### Added — ★ §2.5 的"停下来问"：三个不许替你选的地方

规范要求"一棵树带着 id、而血统表不认识它时，打包器**禁止**静默换 id、也**禁止**
静默沿用 id"。现在它是三处真会拦人的地方：

1. **血统表不认识这个 id**（`init` / `build` / `keygen`）。两种可能的后果**相反**：
   真分身 ⇒ 铸新 id 是正解；"这条记录不在了" ⇒ 铸新 id 是**代价**（所有已同意的
   用户重新同意一次，正在跑的会话变成"未知服务"）。所以停下来，两条路连同后果
   一起说 —— 而**两条路各有可执行的形式**，这是这一版补上的：
   - `init --fork`：这是**另一个**插件，把 id 换掉（旧 id 留在表里，那是它的祖先）；
   - `init --adopt`：这是**同一个**插件，只是记录不在了（本仓库自己那两个插件就是
     这种形状：它们的 id 铸在血统表存在之前）。

   ★ `--adopt` 记下的是**作者的一次声明**，它**不**证明这棵树就是当初铸出这个 id
   的那一棵 —— 打包器判不了那件事（这正是要停下来问的原因）。没有它的话，"这是
   同一个插件"这个答案在工具里**没有可执行的形式**，作者只能手写一张由机器维护的
   表，而那是 typo 走进安全判据的路。
2. **要签的钥匙与血统表里的不是同一把**（`sign`）。要么找回那份私钥，要么 `--fork`。
   ★ 提示里点破了那个想当然的错法：**在这里另铸一把没有用** —— 客户端钉的是旧那一把。
3. **同一个 `(id, 版本)` 算出第二个摘要**（`build`）。见下一条。

### Added — ★ §2.4 的手滑闸：作者机器上的一张发布表

`<home>/releases.json`：这台机器上 `(id, 版本) → 内容摘要`。同一个版本号算出第二个
摘要时**拒绝打包**，并说清"改了内容的任何字节就必须升版本号"。

★ 它挡的是**手滑**，不是攻击 —— 它不进树（每打一次就变，进树就把树弄脏、与 §3.5
打架），所以换一台机器就没有它。与 `install.js` 那条"绝不覆盖内容不同的同版本"
自带的是同一个保留。

★ 它防的那件最容易被忘掉的事，在这一版里**真的会发生**：往树里加一份文件（比如给
老插件补一条血统记录）就是改内容。反过来也成立 —— 它守的正是上面那条"血统表在负载
里"的代价。唯一的例外是"这一版从来没发出去过"，那要显式 `--reuse-version`。

★ 读不动发布表时**不拦，但一定要说出来**（"这一次 §2.4 那条检查没有生效"）——
一条静默消失的防线比没有更糟，它让人以为还有。

### Changed — ★ "站点不验签"这条边界**要收掉了**，接在安装器那一段

> ★ **已经收掉了**，见上面〈集群侧切到包〉那一条与规范 §6.4。

上一版把"站点不验签"写成一条**有断言的事实**（§6 没给站点任何一条验签义务）。
现在决定**要验**，两条理由：安装器是管理员唯一会看到的那句话的出处，而"这个包由
`befd65…` 签名"在没验过时听起来与验过了没有区别；而且**站点自己也有"升级"这个
动作**（`PLUGINS_DIR` 是持久状态），所以它自己也有"上一次"—— 同 id 换了钥匙这件事，
在站点侧**也判得了**。

做法是接**系统 openssl**（`pkeyutl -verify -rawin`），不在 Python 里手写 Ed25519。
★ 在那之前，面向管理员的输出里**不许出现"验签通过"**。记在
[KNOWN-ISSUES.md](docs/KNOWN-ISSUES.md) 的 **S6**（那一段落地时还要顺带给规范 §6
补一条验签义务）。

★ 同时改掉了 `SECURITY.md` 里那段"不做签名站得住"的论证：它挂着三个前提
（单站点 / 站点即作者 / 站点上那份是 root 从源码装的），而"管理员从网站下载作者发布
的包"**已经让其中两条不成立**。按那条论证自己的判据，签名此时从"锦上添花"变成
"唯一能区分作者的东西"。

### Fixed — 两处**测试自己**的脆弱：崩溃伪装成"一条都不红"

变异验证里又撞上同一类假阴性，两次形状不同、后果一样：**防线有没有生效，看不出来**。

1. 变异"写回少一个逗号"产出的是一份**不合法的 JSON**，而用例里的 `JSON.parse`
   抛出去把整个脚本带崩 —— 于是后面每一条用例都不再执行，报告里只看得到一个红。
   修法是让读 JSON 的地方**读不动就返回 `null`**，断言照红、脚本继续跑。
   ★ 这一条上一版已经吃过一次（当时修的是打包器那一侧的解析），而这次它是**被这一版
   新写的用例重新引入的** —— 上一版的修法只补在了那一个调用点上。所以教训不是
   "补一处 try/catch"，而是那条判据本身：**崩溃与"一条都不红"在输出上长得一模一样**，
   新写用例时要照着它检查一遍。
2. 变异脚本自己：它数 `[FAIL]` 行来判"红了几条"，而**脚本崩掉时那条路也会给出
   一个数**。现在**汇总行是唯一的权威** —— 没有汇总行就报"脚本崩了，这一条不算数"。

### Fixed — 生成器里一处写死的常数，在夹具增长时把一条坏包变成了**合法包**

`bad.json` 里那条"`file_count` 比真实条数多 1"用的是**写死的 12**。这一版给夹具树
加了 `lineage.json`（12 份文件）—— 于是那条变异当场变成**一个完全合法的包**。

★ **这次它响得很大声**（三端的用例都报"期望 `record`，得到 `ok`"），所以不是一条
静默失效的防线 —— 但症状指不回根因：看起来像"读方的记录表判据坏了"，实际是生成器
里一个与读方无关的数字。改成相对量（`file_count + 1`），并把理由写进那一条的
`change` 里：**夹具一变，"坏包"必须跟着变**，而写死的常数不会。

### Changed — ★ 版本号收敛成**两套方案**，而且形状不同

这个项目里有两个版本号，从前它们长得几乎一样、还**共用一个解析函数**，于是
"哪一个是哪一个"只存在于人的脑子里。现在分开了，而且**形状不同** —— 形状不同的是
关键：写错了会被**拒绝**，而不是被悄悄当成另一个意思。

| | 形状 | 落在哪 | 谁比它 |
|---|---|---|---|
| **框架版本** | `major.minor`（**两段**） | 四个文件：`client/package.json`、`client/package-lock.json`、`cluster/slurmate`、`cluster/slurmate-sessiond` | 运行期**只有** `engines.slurmate` 这一处。两侧之间**没有**任何版本握手 ← ★ **这一格在 0.7 里变了**，见上面〈版本握手〉那一条 |
| **插件版本** | `major.minor.patch`（**三段**） | 插件的 `plugin.json` | `(id, 版本)` 槽位、会话键、`Registry.list()` 的排序 |

- **`0.6.0` → `0.6`**（四个文件一起）。协议版本、客户端版本、服务端版本是
  **同一个号**，不是三件东西。
- 共同纪律：**禁止前导零**；`minor` / `patch` ∈ `0..255` 且**满则进位**
  （`1.255.255` 之后是 `2.0.0`）；**逐段按十进制字符串比较**（先比长度、再比
  字典序），**禁止转机器整数**。
- ★ **`engines.slurmate` 现在必须写两段**（`>=0.5`）。写三段是一个**看不懂的
  片段** ⇒ 这个插件**不被加载**。仓库里那两个插件、`plugins/README.md` 与
  Python 侧的夹具跟着改了。
- ★ **只校验，不归一化**：谁都不许把 `1.256.0` 改写成 `2.0.0` —— 那是静默改
  版本号，与「一个 `(id, 版本)` 只有一份内容」直接冲突。
- ★ **有两条子纪律是"不会变红"的**（`parseVer` 的不 trim、Python 的不用 `int`）——
  不是漏了用例，是它们今天**不可观测**。如实记着，别把"以为守住了"当成守住了；
  细节见下面〈测试〉那一节。

### Added — ★ 一份两侧**共用**的夹具

`tools/version-fixtures.json`：合法 / 不合法 / 谁大谁小 / 范围满不满足，108 组。
`client/test/boot.test.mjs` 与 `cluster/test-sessiond-logic.py` 第 19.0b2 节读的是
**同一个文件**。

为什么不是各持一份再加一条比对 lint：抄本会漂，而 lint **只看得见已经漂了的
那部分**。（与 `COPY_SKIP` / `PLUGIN_COPY_SKIP` 那一对分工不同：那两个是**生产
代码里的常量**，跨语言没法共用一份，只能靠 lint 守。）

### Fixed — ★ "两侧同一条规则"此前有三处不成立

`engines.slurmate` 是插件作者写的、**两侧各查一遍**的字段。两份实现此前有三处
分家，症状是同一种：**一侧收下、另一侧拒了**，而报错只说"清单不合法"。

1. ★ **Python 先 `strip()` 再匹配**（`need_str`）。于是 `" 1.0.0 "` 在守护进程
   这边合法、在客户端不合法（客户端拿的是原串）。现在两侧都按**原串**匹配，
   报错里也给出原串 —— 否则那句"取值不合法"会指着看上去完全正常的 `1.0.0`。
2. ★ **Python 的 `$` 还匹配"末尾换行之前"**，而 JS 的 `$`（不加 `m`）是严格末尾。
   于是 `"1.0.0\n"` 在一侧合法、另一侧不合法。凡是"整串必须匹配"的地方一律
   `\A … \Z`（插件短名、版本号、ULID 三条正则一起改的）。
3. ★ **两边都放行前导零与超界**：两条正则原本都是 `\d+\.\d+\.\d+`，于是 `1.007.0`
   与 `1.256.0` 都收。现在是同一条 `0..255` 的表达式，两侧逐字相同。

★ 这三条**都是既有缺陷**，不是这次改动引入的 —— 上面那个 `0.6`→`0.6` 只是让
第 1、3 条的后果从"某一侧偶尔多收一个插件"变成"每个插件都可能一侧收一侧拒"。

### Fixed — CI 的"三处版本号"漏掉第四处，而第四处**已经真的落后了**

`client/package-lock.json` 的根 `version` 停在 **0.2.0**，而 `client/package.json`
已经走到 0.6.0 —— 因为 **`npm ci` 不比较**这个字段，所以它一直没人发现。lint 现在
覆盖**四处**，并且不只比字符串、还**比形状**：形状那一半从守护进程 import，不在
workflow 里再抄一份正则（正则的第二个抄本正是这条 lint 要防的那种漂）。

### Added — ★ 一个插件是一个**包**：容器格式、三份实现、一份共用向量

「是不是同一份内容」的判据从**整棵树的摘要**换成**包的内容摘要**。这不是"更整洁"：
整棵树的摘要要求规范钉死符号链接、权限位、空目录、遍历顺序**四件事**，而每一条
都是一次"摘要相同而内容不同"的机会（账本 F18、F24）。包把这些变成**结构性不可能**
—— 格式里没有链接、没有目录条目、权限位写死 `0644`，于是摘要退化成
`sha256(按路径排序的「路径 ‖ sha256(内容)」)`，两种语言各写一遍会逐字一致。

★ **这一版不动线、不动布局，也不改变任何一条运行时的行为** —— 它落的是**格式**
与**读包的能力**。站点仍然报 `files`、客户端仍然按 `files` 取，一个字没改。

- **容器格式成为规范的一部分**（**附录 A，规范性**）。
  ★ 它现在住在 [`docs/PLUGIN-CONTAINER.md`](docs/PLUGIN-CONTAINER.md) 里，
  编号 `A.1`–`A.5` 沿用 —— 源码注释里那些「附录 A.4」指的就是那里。
  理由不是"别人要照着实现"（那个承诺已经在 §0 删掉了），而是两条更结实的：
  ① §3.6 的"不隐藏"承诺**必须有可验证的形式** —— 作者要能拿 `packer inspect`
  的输出对着规范逐行核对；② 三份实现要有一处共同指向的定义，否则它们会各自漂。
  附录里同时钉了**拒绝的理由词表与判的次序**（A.4）—— 次序也是契约：一个包同时
  犯两条时，先判哪条决定了报出来的是哪个词。
- **`packer/`（新目录）**：单文件、零依赖的打包器（作者的机器上的工具，
  `deploy.sh` 永不打包）。动词 `init` / `build` / `verify` / `inspect`。
- **Python 读方**（守护进程）＋ `--extract-package <包> <包内路径>`：集群侧读包的
  **唯一**入口 —— 将来部署脚本既要包里的 `job/start.sh` 去织作业脚本，就走它，
  于是"包怎么读"只有 Python 这一份实现。
- **客户端读方**（`client/src/main/plugin-package.js`）。★ **这一版它没有调用方，
  这是刻意的**：读包的能力必须先于任何一条线落地 —— 反过来（先让站点发包、再让
  客户端学会认包）会在中间开一个窗口，那个窗口里客户端拿到的是它读不懂的字节，
  而它唯一能说的话是"校验不过"。
- **`tools/conformance/`**：一份固定的输入树 + 期望的**包字节** + **23 条坏包**
  （每条带一句"改了哪几个字节"和一个期望的**理由词**）+ 一组签名夹具。三端读
  **同一批十六进制**。★ 坏包是**常量**而不是文件：三端拿到的字节必须逐字节相同，
  而"各自用自己语言里的库造一个坏包"必然造出不同的字节 —— 那时"三端都拒了"
  就退化成三句各说各的。对价是**变异只在 `generate.mjs` 里造**（一份引擎），
  且 `bad.json` 每一条都带着 `change` 与 `why`。
- **`*.splug` 不进 git**（`.gitignore`）：二进制包进 git 等于代码评审死掉 ——
  今天 `plugins/code-server/client/index.js` 是可以逐行看的。包是**构建产物**。

### Fixed — ★ 打包器自己的一个 bug：摘要忘了排序（用例当场抓住）

`contentDigest` 早先直接按**喂进来的次序**拼哈希，排序写在调用方。于是它只对
"打包器自己排过序的那条路"成立 —— 而**读方**拿到的记录表是**别人给的**：一个手写
的包完全可以把记录按任意次序排。那会算出另一个摘要，而**签名盖的是摘要** ⇒ 症状
是"签名验不过"，排查的人会去查钥匙。现在排序在 `contentDigest` 内部，两侧的用例
都钉着它（"换掉两条的次序 ⇒ 摘要不变"）。

### Fixed — `site-plugins.js` 的大小写折叠改回 ASCII-only

判"两条路径只差大小写"用的是 `p.toLowerCase()` —— **全 Unicode** 的折叠会把
`İ`（U+0130）折到 `i`、把 `K`（U+212A KELVIN）折到 `k`。而这是一条**拒绝**规则：
折叠口径不一致就会出现"一边收、一边拒"，也就是**同一个插件在一台机器上装得上、
在另一台上装不上**，而报错里一个字都不会提到大小写。现在两侧都用
`plugins.foldAscii`（只折叠 `A`–`Z`）。

### Changed — 规范补了四处（都不是推翻既有要求，是写完整）

1. **§3.3 补"两条路径禁止完全相同"** —— 原先只禁了"只差大小写"。两条逐字相同的
   路径带着两份不同内容时，内容摘要**有定义**（它按路径排序后拼接），可**解出来的
   树是歧义的**（哪一个字节该落到那个路径上？）。
2. **§3.3 补"路径必须是合法的 UTF-8"** —— 不合法的字节会被解码器**悄悄换成**
   `U+FFFD`，于是摘要算的是一个被替换过的字符串，两端算的不是同一份东西。
3. **§3.3 与 §3.6 有一处字面冲突，现在读得通了**：真实源码树几乎总有一份
   `.gitignore`，而它是 §3.3 禁止出现的路径 —— 于是"解包得到的树必须与源码树逐字节
   相同"永远不成立。唯一说得通的读法是"**源码树 = 过掉跳过集之后的那棵树**"，
   而**减去的东西必须被说出来**（打包器要把跳过了什么打出来）。
4. **§2.3 补"校验，不归一化"** —— 谁都不许把 `1.256.0` 改写成 `2.0.0`。那是静默
   改版本号，与 §2.4 直接冲突。

### Changed — ★ "站点不验签"是一条**有断言的**事实

三份实现的验签能力并不对等，而这是刻意的：

| 那一端 | 验签吗 |
|---|---|
| 客户端、打包器 | ✅ 验 |
| 守护进程（Python） | ❌ **不验** —— §6 没给站点任何一条验签义务 |

`bad.json` 里那两条"只有会验签的一端才拒得了"的坏包带着 `needs: "verify"`，
而守护进程的用例**断言它确实收下了它们** —— 把一条被省掉的检查写成一个有断言的
事实，比让它悄悄躺在那里好。代价与后果记在 [KNOWN-ISSUES.md](docs/KNOWN-ISSUES.md)
的 **S6**（其中最关键的一条：安装器**不许**说出"验签通过"这四个字）。

★ **这条边界后来改了**：站点侧的验签决定要做，而且**已经做了**（接在安装器那
一段）—— 见上面〈"站点不验签"这条边界要收掉了〉与〈集群侧切到包〉。

### Changed — ★ 跳过表从**两端**变成**三端**，而这次它俩是同一条规则的两个方向

`packer/slurmate-packer.js` 也有自己一份 `COPY_SKIP` —— 没办法，它要"下载这个
文件夹就能用"，import 不到客户端里的常量。CI 那条 lint 因此扩展成三端逐字比对。

★ 而它与 §3.3 的"禁止出现下列跳过集合里的任何一段"是**同一条规则的两种用法**：
打包器**排除**它们，读方**拒绝**含它们的包。同一份名单，两个方向 —— 于是这条
lint 现在挡的是一件比"抄本漂了"更具体的事：**排除的那些必须正好是拒绝的那些**。

★ 顺带一处**如实记下的教训**：输入树里有一份我起名叫 `cases/nul.bin` 的文件，
被打包器当场拒了 —— `NUL` 是 §3.3 禁止的 Windows 保留名。规则是对的，名字改成了
`cases/zeros.bin`。规则第一次生效是在它自己的作者身上。

### Changed — ★ 插件由**站点分发**，客户端那一步人工动作没有了

这是这一版唯一的一件大事，其余改动都是它的后果。

在此之前，「站点开了 X 而本机没有 / 版本对不上」是**用户自己要解决的问题**，
而客户端给出的提示是「升级客户端」—— 那句话在插件由站点分发之后才是对的；
在此之前它是**假的**，因为**没有任何东西会从集群取插件文件**。

```
集群   <prefix>/share/slurmate/plugins/<目录名>/     ← deploy.sh 装好的那份，一字未改
         │  plugins：每项带 files[{path,size,sha256}]，顶层带 limits
         │  plugin_file：一份一份取（base64）
         ▼
客户端 ~/.slurmate/site-plugins/<id>/<版本>/          ← 站点池（一份内容），按引用计数回收
       ~/.slurmate/site-plugins/.sites.json           ← 快照表：哪个站点要哪个版本
       ~/.slurmate/.site-staging/<随机>/               ← 每次尝试唯一；**兄弟目录**
       ~/.slurmate/plugins/<id>/<版本>/                ← 本机池，**用户自己的**，要开关才加载
```

★ **两个池，不是一个**：站点池是**站点拥有的**（可以回收），本机池是**用户拥有的**
（永不自动动它）。合并的后果很具体：回收会去删用户手装的那一份 —— 而它不在任何
站点记录里，引用数天然是 0。

★ **`deploy.sh` 一行都不用改**（除了下面那条信任门）：集群侧的改动**全在 RPC 层**。

### Added — ★ 同意闸：这一版存在的前提

客户端侧的插件代码**跑在主进程里**（完整 Node 权限、没有沙箱）。所以这是本系统
第一条**从远端来、且会变成可执行代码**的路 —— 它必须有一道闸。

- 每个 `(id + 版本 + **整目录摘要**)` 第一次都要用户点一次同意，**没有例外**
  （不给"纯声明式插件免同意"开口子：一个恶意 `plugin.json` 也在往你机器上放东西）；
- 台账是 `~/.slurmate/config.json` 的 `trustedPlugins`（**全长 64 位**摘要，
  与 `hostKeys` 同一先例：一个我自己算出来的值，记在本地，下次拿它比对）；
- **闸的落点是"下载后、暂存验完、换入之前"**：更早没有摘要可同意（摘要从字节算
  出来），更晚就已经执行了；
- 界面上把话说满：**没有进程隔离、这段代码会在你这台机器上跑**，第二次之后显示
  旧摘要 → 新摘要。

### Added — 站点池、引用计数、以及"站点不报它"不等于"删掉它"

`~/.slurmate/site-plugins/.sites.json` 记着每个站点当前要哪个版本。规则**只有一条**
——**指针只在成功拿到新版本时移动，绝不清空** —— 而它同时给出三件事：

| 情形 | 结果 |
|---|---|
| 站点把 X 从 1.0.0 升到 1.1.0 | 旧版引用归零 ⇒ 回收（"装新删旧"不是一条单独规则） |
| 站点**关掉**或整个移除 X | 指针停在原处 ⇒ **留着不删** |
| 这一次**失败** | 同样不动 ⇒ 一次抖动不会把上一份按"没人要了"回收掉 |

★ **不知道谁在引用的时候，唯一安全的动作是什么都不删** —— 记录读不出来时只报告、
不回收（与 `deploy.sh` 的「有东西但没有标记 ⇒ `die`，不删」同一个先例）。

★ **回收之前先写记录**（唯一会真丢数据的地方），而且活会话用着的版本另外受保护
（那一步要问守护进程的 `list`，光看本地会话不够：客户端重启会接回旧会话）。

### Changed — ★ `loadDir()` 就是 `require()`，所以它拆成了两趟

**这是让同意闸成立的结构性前提。** `plugins/index.js` 的 `loadDir()` 在读到
`client/index.js` 时**当场** `require()` —— 也就是说"扫一遍池"**就是**执行。
于是"对账结束时才 reload、没同意的不激活"这句话本来是**空的**。

现在拆成两趟：`inspectDir()`（读清单、校验、**只编译不运行**、走一遍目录算摘要）
与 `activatePlugin()`（真正 `require()`）。没同意的仍然出现在列表里（用户要看得见
才能点同意），但**一个钩子都没有**，而且 `resolve()` 会拒绝它。

用例在 `client/test/boot.test.mjs`：一个在被加载时写标记文件的插件，
**对账跑完、点同意之前**，那个标记文件必须不存在。

### Fixed — ★ F18：插件摘要只覆盖两个文件

`digestOf(manifestRaw, clientRaw)` 只把 `plugin.json` 与 `client/index.js` 算进
摘要，而插件目录里可以有别的文件（sshd 的 `client/sshconfig.js` 就是）。后果不是
报错，是**每次都说成功**。

现在改成**整目录摘要**，规格写死：按相对路径排序（`/` 分隔）、先写项的类别
（`f` / `l` / `d`）、**权限位入哈希**、**空目录入哈希**、符号链接写 `readlink` 的
**字符串**而不跟随。最后一条是最精确的那个不对称：目录 A 里 `client/x.js` 是链接
指向 `../y.js`、目录 B 里同路径是内容恰好那 8 个字节的普通文件 —— 摘要会**完全相同**，
而行为天差地别。

### Fixed — ★ `deploy.sh` 的源文件信任门漏掉了 `client/`

信任门（`source_is_trusted()`）以前只看 `plugin.json` 与 `job/start.sh`，理由是
"插件目录里其余的都不装、也不读"。**分发接上之后那句话不成立了**：`client/` 整棵
子树会被发到每一台客户端并在那里 `require()` —— 于是"某个普通用户能改写它"的后果
从"没什么后果"变成**"他的代码在每个用户的工作站上跑"**。

名单扩到**整个插件目录**：判据不再是"谁读它"，而是"**它会到谁的机器上、以谁的身份
跑**"。用例 `cluster/test-sessiond-logic.py` 第 19.13 节把那函数**抠出来真跑**、
断言"会分发的每一份文件都在名单里"——改之前它是红的。

### Added — 集群侧：`files` / `limits` / `plugin_file`

- `plugins` 每一项多了 `files`（`[{path,size,sha256}]`），顶层多了 `limits`；
- 新 op `plugin_file`（`id` / `version` / `path` → 一份文件，base64）；
- **"站点支不支持分发"的判据是 `files` 字段在不在**（能力缺席，一个稳定的协议
  事实），**不是"这次下载失败了"**（瞬时事实）。把两者合并等于给一个能让下载失败
  的人一个把用户降级到旧本地副本的开关 —— 攻击成本从"改内容"降到"让下载失败"；
- `files[].sha256` 与 `limits` 都是**自述**：前者只用来与**客户端自己从磁盘重算**
  的值比，后者只用来**收紧**客户端自己那份硬上限；
- 路径安全靠**先建表再按表取**：`path` 只是索引表的键，从不参与拼路径 ——
  于是"路径穿越"这个词从等式里消失；
- 三个新 kind：`3 plugin_unknown` / `3 plugin_file_unknown` / `4 plugin_file_too_large`
  （外加 `9 plugin_file_changed`：管理员就地换了文件）。**绝不截断**。

### Changed — 两处跨语言/跨文件的一致性，收敛到一个来源

- **`COPY_SKIP` 全客户端只剩一份**（在 `plugins/index.js`，安装器与摘要都从它引），
  `.github/workflows/checks.yml` 与 `test-sessiond-logic.py` 第 19.12 节的锚点跟着
  移到那里；
- 守护进程那份 `PLUGIN_COPY_SKIP` 与它逐字比对 —— 漂开之后的症状是"同步一直失败"，
  而报错里一个字都不会提到是这两个集合分家了。

### Fixed — `missing` 不再把"站点关掉的插件"说成"你该升级客户端"

`op_plugins` 按协议**必须报全部插件包括 `enabled:false` 的**，而客户端的
`plugins.missing` 是拿站点清单逐条减出来的 —— 于是"站点关掉一个插件"会被界面
永远说成"站点有而本机没有 ⇒ 升级客户端"。现在它只看 `enabled` 的。

### Added — 界面：三个新节，一处删掉的入口

- **站点分发**一节：本机已有的站点插件、**每个版本被哪些站点要**（它解释了"为什么
  这台机器上有两个版本"）、上次同步时间、「重新同步」；
- **待同意**是**独立的一块**，不是"起不来"的第五档 —— `WHY_NOT_RUNNABLE` 那四句话
  说的是"这个插件**起不来**"，而"还没同意"是"**还没到手**"；
- **开发者**一节：`☐ 也加载本机插件目录（开发用）` + 池路径；**勾上之后**才出现
  「从目录安装…」那几个入口 —— 于是"禁止自装"在默认路径上是真的，而本机开发仍有
  一条说得出来的路；
- 「从目录安装…」「重新扫描」从底部那一行**移进**了开发者一节。

★ `test/renderer.test.mjs` 里那条「`panel.js` 不按 source 给插件贴来源标签」的检查
**按它自己写的方式翻面了**：它当年就写着"将来真的有了来源标签，正确的做法不是删掉
这条检查，而是按当时的 sources 把标签加回来，再把它改成一条正面用例"。

### Added — 演示后端现在分发**仓库里真正的** `plugins/`

以前演示站点报的就是本机池里的插件，于是「站点有而本机没有」这条**最重要的**路径
在演示里**永远走不到** —— 而演示模式恰恰是这个项目里唯一能造出那些状态的地方。
现在它从仓库的 `plugins/` 读真文件（真 sha256、真字节），客户端真的走一遍
下载 → 暂存校验 → 同意闸 → 换入。池空时界面上是「本站要给你两个插件，但都还没
经过你的同意」—— 一句真话。

演示调试也补了三个新开关：站点不支持分发 / 插件太大 / 站点在限流。

### Fixed — `slurmate rpc` 的响应超限文案

响应超过 1 MiB 时 `json.loads` 必失败，于是客户端把它归成 `daemon_unreachable`
（code 5）**并退避重试** —— 一次"文件太大"会被报成"控制节点上的守护进程没有响应"。

### 文档

- [SECURITY.md](SECURITY.md) 新增〈站点分发的插件：四条护栏的对账〉：**逐条说清
  做了哪一条、用什么替代了哪一条、还欠哪一条**，以及不做签名的三个前提与
  "前提一破就必须回头做"的判据。**答案不是"四条都做了"**（那是假的）。
- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) 的〈插件从哪来（分层）〉从"下一阶段"
  改成对那一句"否则它就是一个安静的后门"的**正面回答**；同时改掉一处变假的说法：
  分发**不经过** `installFrom`。
- [docs/PROTOCOL.md](docs/PROTOCOL.md) 升到 v0.6，并**补上 v0.5 漏登记的三个 kind**
  （`missing_service_kind` / `service_kind_disabled` / `service_kind_no_job`）——
  一张不全的错误码表，症状是下一个人照着错的表写代码。
- [docs/KNOWN-ISSUES.md](docs/KNOWN-ISSUES.md)：**S1 与 F18 离开账本**（各带一条能
  真的红的用例）、**S2 收窄成一条**（只剩进程隔离）、F20–F24 新账、**U7**（真集群上
  的一次完整分发从没跑过 —— 包括那个 1 MiB 上限到底够不够）。
- 五处「将来站点分发走的也是这条路」从**将来时改成现在时，并且改成准确的样子**
  （分发走另一条路、落另一个根）。
- ★ [docs/PLUGIN-SPEC.md](docs/PLUGIN-SPEC.md) 新增 **§2.3.1〈你还会写第二个版本
  号：框架版本〉** —— `engines.slurmate` 这个字段**从前一个字都没写过**，而插件
  作者必须知道它要写两段、以及写三段会被拒。
- ★ [docs/PROTOCOL.md](docs/PROTOCOL.md) 开头的〈协议版本与变更〉改成先讲清
  **这个号是什么、什么形状、落在哪四处**，再讲变更表。
- ★ [SECURITY.md](SECURITY.md) 与 [CHANGELOG.md](CHANGELOG.md) 顶部
  **不再声称"遵循语义化版本"** —— 那是错的（语义化版本要求三段，而这个号是两段，
  而且 `minor` 有上界、`major` 无上界）。`0.2.0` 及更早的历史标题保持原样。

### 测试

客户端 **175 → 213** 条（新增 `test/site-plugins.test.mjs` 27 条、版本号夹具 1 条），
集群侧 **390 → 407** 条（新增 19.13 节与 19.0b2 节）。变异验证 17 处，每处只让对应
用例变红。

★ 其中两处**第一次没红**，而原因都在用例上：一条断言太松（"被拒了"就行，
而拒绝的理由其实是另一条规则碰巧兜住了），一条**因为错误的理由通过**（前置的
另一条用例已经把插件装好了）。已加强 —— 这两条是这一轮最有价值的发现。

★ **版本号那一批又做了 9 处变异**，结果比上一批更值得记：

| 变异 | 红了什么 |
|---|---|
| JS 段比较改成 `Number` | 夹具那条（2^53 那两组） |
| JS 的 `0..255` 正解放回 `[0-9]+` | 夹具那条 |
| JS 的 `cmpVer` 段数不同返回 0 而不是抛 | 夹具那条（`assert.throws`） |
| Python 的 `need_str` 又先 `strip()` | 19.0b2 那三条走真清单的用例 |
| Python 的 `\Z` 退回 `$` | 两张形状表 + `"1.0.0\n"` 那条 |
| `parseVer` / `_ver_segments` 改成 `.trim()` | ★ **一条都不红**（见下） |
| Python 的段比较改用 `int()` | ★ **一条都不红**（见上） |
| 客户端版本、守护进程版本、插件清单退回三段 | 一大片（宿主版本对不上 ⇒ 插件集体不收；集群侧还**崩**在 §18） |

★★ 两条**不会变红**的，都已如实写进代码注释与上一节：一条是**纪律本身就不
可观测**（`parseVer` 的 trim —— 三处调用方喂进去的都过了正则那道门），另一条是
**Python 的 `int` 在数值上是对的**。把"以为它被守住了"当成"被守住了"，比一条
明写的纪律更糟，所以这两条宁可写在明处。★ 顺带：夹具表测的是**正则**，
"清单里的版本号按原串匹配"是**另一条**规则（住在 `inspectDir` / `need_str` 里），
所以客户端那侧补了一条**走真清单加载**的用例 —— 把校验改成 `.trim()` 时，
**只有它**变红。

★ **包的那一批又做了 15 处变异。** 客户端 **213 → 223** 条，集群侧 **407 → 429**
条（新增 19.14 节），打包器 **46** 条（新的一套，`packer/test-packer.mjs`）：

| 变异 | 红了什么 |
|---|---|
| 打包器的 `contentDigest` 不排序 | 打包器「换掉两条的次序 ⇒ 摘要不变」 |
| 打包器的排序改用 JS 的 `<` | 打包器 6 条（字节、摘要、次序、签名、inspect） |
| 客户端的 `contentDigest` 不排序 | 客户端「摘要按 UTF-8 字节排序」 |
| 路径规则里去掉 Windows 保留名 | 坏包那一条（`path` 那一组） |
| 客户端去掉「长度必须精确」 | 坏包那一条（`length` 那一组） |
| 客户端记录表越界不判 | 坏包那一条（`record` 那一组） |
| 客户端 `foldAscii` 退化成全 Unicode 折叠 | 客户端「大小写折叠是 ASCII-only」 |
| 守护进程 UTF-8 解码用 `errors="replace"` | 19.14 的 21 条理由词 |
| 守护进程不判签名块形状 | 19.14 的 21 条理由词 |
| 守护进程 `foldAscii` 退化成 `str.lower()` | 19.14「大小写折叠是 ASCII-only」 |
| 守护进程的内容摘要不排序 | 19.14「倒着喂进去算出同一个摘要」 |
| 打包器不排除树里的 `.splug` | 打包器「上一次打出来的 .splug 没进负载」 |
| `init` 改成 `JSON.parse` + `stringify` | 打包器 4 条（`\u` 转义、`1e3`、键序、逐行比） |
| 写回少一个逗号 | 打包器 5 条（★ 见下） |
| 跳过表里打包器少一个 `.github` | 打包器 2 条（含「三端一致」那条） |

★★ **倒数第二条第一次报的是"一条都不红"，而那是错的。** 它是一个**假阴性**，
而且错在**我这一侧的工具**上：少一个逗号写出的是一份**不合法的 JSON**，用例里
那句 `JSON.parse` 直接**抛**出去，整个测试脚本当场崩掉 —— 于是它后面每一条用例
都没跑，而我从输出里一条 `[FAIL]` 也抠不到。它看起来像"这道防线不存在"，
实际是"这道防线把测试跑挂了"。**两处都改了**：用例把解析放进 `try` 并**报一条
失败**（而不是崩），变异脚本也补上了"没有表尾 ⇒ 脚本崩了"这一档。改完再跑，
红了 5 条。

> ★ **一条把测试跑挂的防线，与一条不存在的防线，在输出上长得一模一样。**
> 这一条单独记在这里，因为它比任何一个具体的 bug 都更值得记住：一个崩溃的用例
> 在 CI 上还容易被读成基础设施故障。

★ **还有一处是"用例当场抓住我自己写的 bug"**：打包器的 `contentDigest` 早先按
**喂进来的次序**拼哈希（排序写在调用方）。它对"打包器自己排过序的那条路"成立，
而**读方**拿到的记录表是**别人给的** —— 一个手写的包可以把记录按任意次序排，
于是同一个包算出两个摘要，而**签名盖的是摘要** ⇒ 症状是"签名验不过"，
排查的人会去查钥匙。修好之后专门补了一条断言钉它。

### 尚未做（下一阶段）

> ★ **这一节的现状（后来补记）**：集群侧、协议上的加法过渡、以及**客户端那一半**
> **都做完了** —— 见上面〈集群侧切到包〉〈整包那条路〉〈客户端按包安装〉。
> 而最后那件事（**删掉 `files` 与 `plugin_file`**）**已经在 0.7 里做了**。
>
> ★ **那道"等 U7 真集群分发跑过之后再删"的闸撤了**，理由记在
> [docs/KNOWN-ISSUES.md](docs/KNOWN-ISSUES.md) 的 U7 那一节：**它当初的推理比它
> 读起来弱** —— 客户端从 0.6 起就优先走包，所以 U7 无论删不删 `files`，跑的
> **都是** `plugin_package` 那条路；那条退路真正保护的是**一个老的客户端二进制**，
> 而那不是一次实测能验的东西。下面每一条后面都标了现状。

- **把安装路径切到包上。** 这一版落的是**格式与读包的能力**，而**线一个字节都
  没动**：站点仍然报 `files`、客户端仍然按 `files` 取、安装器仍然收目录。
  顺序是刻意的（读包必须先于任何一条线落地），代价是这一版**没有能演示的东西**。
  要做的四件：集群侧的安装器与 `PLUGINS_DIR` 切到 `<ULID>.splug`（**带一次旧目录
  迁移** —— 否则 root 拥有的、含客户端代码的旧副本会永久残留且不可见）；
  `needs_job` / `source_dir` 重定义（它们今天从 `plugin.json` 所在的**目录**推出来，
  切包之后会**恒为假** ⇒ 每个插件的按钮静默变灰、没有任何一条错误）；
  客户端按包安装并修那个"池里有一份但台账对不上 ⇒ 界面上彻底看不见"的洞；
  协议**加法**过渡（同时报 `files` 与 `package`），最后一段才删 `files`。
  ★ **已做**：安装器、`PLUGINS_DIR` 切包、旧目录迁移、`needs_job` / `source_package`
  重定义（判据改成"包里的负载有没有 `job/start.sh`"）、协议那一步（同时报
  `files` 与 `package`、整包一次取回来、抬 CLI 的读上限），以及**客户端那一半**
  （优先走 `package`、池里两样挨着、那个洞、§5.3 的撤回同意、`alg` 字段 ——
  见上面两条 `Added`）。**都做完了**，而最后那一段（删掉 `files` 与 `plugin_file`）
  在 **0.7** 里做了 —— 见本文件顶部那一条。
- **签名的密钥那一半**：`packer` 的 `sign` 动词、血统表、客户端**按 id** 的钉表
  （独立于同意台账 —— 合并等于给"分身必须铸新 id"那条机械判据配一个重置按钮）。
  字节格式与验签已经就位。★ 站点的读方**刻意不验签**（§6 没给它这条义务），
  而它的后果（安装器不许说出"验签通过"四个字）记在 KNOWN-ISSUES 的 **S6**。
  ★ **已做**：`sign` 动词、血统表、客户端的钉表（阶段 3），以及**站点侧的验签**
  （安装器接系统 openssl + 按 id 记住签名者）—— 规范 §6.4 同步补上，S6 也从
  〈结构性的欠账〉挪进了〈已经不再是问题的〉（那几条边界留在原地）。
- **`PLUGIN_TOTAL_MAX_BYTES` 与 base64 膨胀 4/3 对不上**：整包一次发时那个
  "1 MiB" 是一句假话（要么把上限降到约 780 KiB，要么抬 CLI 的读上限）。
  今天最大的插件约 53 KB，不会立刻炸，但那个数要改对 —— 与切包那一段一起。
  ★ **已结清**：整包一次发接上之后，CLI 的读上限从 1 MiB 抬到 4 MiB
  （`RPC_MAX_RESPONSE_BYTES`），而上限之间的关系有了一条**跨文件用例**钉着
  （19.11d）—— 从前那种"数字改了、另一侧没跟上"不会悄悄回来。
  这一版加的 `PLUGIN_PACKAGE_MAX_BYTES`（**一次取整包**那条链路的硬上限）由安装器
  与 `op_plugin_package` 两侧都拦。
- **签名**（论证与三个前提见 SECURITY.md）；**进程隔离**（四条护栏里仅剩的一条，
  需要一次重构）。在它做到之前，**同意界面的措辞就是唯一的安全边界**。
- **真集群上一次完整分发都没跑过**（U7）：那 4 MiB 的响应余量够不够、限流桶够不够
  用、单线程守护进程扛不扛得住逐文件传输、NFS 上 `rename` 与 `fsync` 的口径 ——
  一件都没验过。
- 其余未修项、未实测项与结构性欠账，见 [docs/KNOWN-ISSUES.md](docs/KNOWN-ISSUES.md)。

---

## [0.5.0] — 未发布

> 三处版本号当时都是 **0.5.0**。**两端协议不兼容，必须一起升。**
>
> 下面两节（0.4.0 / 0.3.0）都**从未发布、从未部署过** —— 从 0.2.0 升上来的站点
> 看 0.6 那一节就行，这里的内容全在里面（并且被 0.6 改掉了不少）。

### Changed — ★ 两个插件从基座里**彻底**搬出去，成为独立项目

这是这一版唯一的一件大事，其余改动都是它的后果。

```
Slurmate/
  client/    基座。src/main/plugins/ 里只剩框架（注册表 + ulid + 安装器）
  cluster/   基座。守护进程 / CLI / 作业模板 / 部署脚本
  plugins/   ★ 两个插件的**独立项目**。基座不依赖它们，客户端打包不含它们
```

**「基座里没有任何一个插件的名字」在两端都成立，而且是可机检的** ——
有一条用例逐字断言作业模板里不许出现任何插件名，另一条断言客户端渲染层里
不许出现插件名字面量。

在此之前，这两个插件**长在基座的身体里**：一半是 `client/src/main/plugins/` 下的
内建模块，一半是 `cluster/run.sbatch` 里三百行 bash，还有一部分以「通用能力」的
名字躺在基座深处（`login.js` 里写死的 code-server 登录契约、唯一消费者是 sshd 的
`sshconfig.js`、扮演假 code-server 的 `demo-server.js`）。而按设计，**插件应该是
单独的项目** —— 所以这一版把这条线钉死。

### Added — 一个插件是一个目录，三半

```
<插件目录>/
  plugin.json        身份、版本、两侧共读的声明     ← 必须有
  client/index.js    客户端侧代码                  ← 可以没有（= 声明式插件）
  job/start.sh       作业侧代码                    ← 可以没有
```

**`(id, 版本)` 把三半认亲**：会话把它起时那一刻的 `<id>@<版本>` 记进会话文件，
客户端拿它去自己的池里找配套的那一半。于是**站点升级插件不影响已经跑着的作业**
—— 那些作业用的仍是旧的那一版实现。

★ **目录名不参与任何判定。** 身份来自清单里的 `id`（铸造的 ULID），版本来自
`version`，站点内的短名来自 `name`。

### Added — 装到哪：两侧各有一个安装点，加插件都不用改代码

| 侧 | 装到哪 | 怎么装 |
|---|---|---|
| 客户端 | `~/.slurmate/plugins/`（**池**） | 界面上的「从目录安装…」，或直接拷一个目录进去再点「重新扫描」 |
| 集群 | `<prefix>/share/slurmate/plugins/` | `sudo bash cluster/deploy.sh --plugins-src DIR` |

**加一个插件 = 放一个目录 + 跑一次安装动作**，不用改守护进程的源码，也不用改配置
文件。守护进程仍然要 root 重部署一次，但那是**安装动作**，不是**改代码**。

★ 本版做的是**第 1 层（本地安装）**，而它是第 2 层（站点分发）的**同一条路**：
客户端的 `installFrom(dir)` 不关心那个目录是从哪来的。**站点分发还没有实现**
（没有"从集群取插件文件"那个 op），见〈尚未做〉。

### Changed — ★ 作业侧：**部署时编织**，不是运行时 `source`

`cluster/run.sbatch` 从 **748 行降到 479 行** —— `start_code_server` /
`start_sshd` / 公钥校验 / 主机密钥 / banner 嗅探 / sshd_config 全文，以及三处
散在分支之外的插件专属代码，全部搬进 `plugins/<名字>/job/start.sh`。宿主只留：
端口挑选重试、心跳、口令、日志、`write_session`、`cleanup`/`trap`、以及一个
`plugin_call` 分派器。

`deploy.sh` 对**每个插件单独织一份**：把它的 `job/start.sh` 拼在模板里那个
`# @@SLURMATE_PLUGIN_BLOCKS@@` 标记处（每份成品里**恰好一个**插件块），装到
`<prefix>/share/slurmate/jobs/<ULID>.sbatch`（root:root 0644）。提交时用哪一份由
守护进程按 `service_kind` 定 —— `build_sbatch_argv` 的末项就是那条路径。

★ **一个插件一份，不是所有插件织进一份。** 同处一份文件时，插件里任何一行**不在
函数里**的代码（一个多余的 `set -e`、一个变量赋值）都待在主流程中间 —— bash 自上
而下解析整个文件，那一行于是在**每一个**作业里执行，**不管用的是哪个插件**。一个
插件的笔误因此可以改变所有其他插件的作业行为，而症状指不回任何一个文件。拆开之后，
一个插件的代码连"被另一个插件的作业解析到"的机会都没有。
（`test-sessiond-logic.py` 第 22 节真的跑两个插件来钉这一条：带顶层语句的那个插件
执行了它自己那行，而在另一个插件的作业里**没有**执行。）

文件名用清单里的 `id`（ULID）而不是短名：`id` 是插件的**身份**（全球唯一、铸出来
就不变），短名只是本站的标签、可以改。代价是目录列出来看不懂 —— 补偿是 `deploy.sh`
完成摘要里那张对照表与 `--check` 的逐插件输出，两者都是**派生**的，不是第二份真相。

★ **为什么不是运行时 `source`**（这是个必须写清楚的决定）：`sbatch` 拿到的是
**路径**，但计算节点上的 slurmd 从**自己的 spool** 取脚本执行 —— 本系统从来没有
让计算节点打开过 `<prefix>/share/slurmate/` 里任何一个文件。改成运行时 `source`
会引入本项目**有史以来第一个**「共享目录对计算节点可见」的前置条件，而那个事实
**在登录节点上永远验证不出来**（文件在那儿必然存在）。

编织让不确定性归零，并且把失败点**前移**到部署期：一个语法错的插件脚本让
`deploy.sh` 当场中止，而不是变成用户排完队之后的一次失败会话。
**从一份拆成 N 份没有改变这条论证** —— 仍然是部署期把内容写进文件，`sbatch` 仍然
只拿到一个路径，计算节点仍然不需要看见 `<prefix>/share/slurmate/`。

### Added — 作业侧的插件契约

宿主与插件之间**只有一条接口：函数名**。

| 钩子 | 必须吗 | 说明 |
|---|---|---|
| `start_<短名>` | **有作业侧就必须有** | 返回 0 = 起来了，**并且必须把 pid 写进 `$SVC_PID`** —— 心跳的存活门、`cleanup` 里的杀进程、主流程最后的 `wait` 三处都读它。不写它，作业会在"会话就绪"那一行之后**立刻结束** |
| `precheck_<短名>` | 可选 | 非 0 → 宿主写墓碑并以 **24** 结束，**不会开始挑端口** |
| `cleanup_<短名>` | 可选 | 在宿主最后一行**之后**、NFS 补写**之前**被调 |

外加一个关联数组 `PLUGIN_SESSION_FIELDS[键]` 往会话文件加字段 —— **转义由宿主
统一做**。让每个插件自己拼 JSON 片段等于把转义责任推给每一个插件，而转义写错的
下场是整份会话文件解析不了。

★ 命名规则是 `<动词>_<短名>`（短名里的 `-` 写成 `_`），插件内部辅助函数一律用
`_<短名>_` 前缀。宿主自己的函数都是**没有后缀**的动词，两套不会撞。这条规则
**正好等于** `PluginSpec.job_entry` 一直以来的值 —— 那句"只用于文档与自检输出"
的注释因此变成了真契约。

★ **比计划里少了三个钩子。** 计划里有 `init_`（环境净化）、`session_`（塞会话
字段）、`auth_`（覆盖认证方式），实现时都去掉了：`init_` 那段 `unset CODE_SERVER_*`
放在 `start_code_server` 里更准确（真正在乎它的是即将被启动的那个进程）；
`session_` 被 `PLUGIN_SESSION_FIELDS` 取代（声明式比钩子好）；`auth_` 是多余的
（守护进程本来就下发 `SLURMATE_AUTH_MODE`，插件在自己的 `start_` 里读它即可）。
**契约越小越好，尤其是"宿主与插件之间只剩函数名"这条边界。**

### Added — 部署期的三条硬断言

`deploy.sh` 在编织前对每个 `job/start.sh` 断言，任何一条不过就**当场中止部署**：

1. **没有 shebang、没有 `#SBATCH`** —— 它们会被拼在作业脚本的中间，而 Slurm 只解析
   脚本**开头**那一段连续的注释。带了不是报错，是"写了但静默不生效"。
2. **有作业侧就必须定义 `start_<短名>`** —— 没有它，作业会在用户**排完队之后**才
   失败，而日志里那句"候选端口全部失败"指不回这个文件。
   （**没有 `job/start.sh` 本身不在中止之列** —— 那是合法状态，见下。）
3. **每一个函数名都带命名空间** —— 一插件一份之后，插件**之间**不会互相覆盖了，
   但插件盖掉 `log` / `cleanup` / `write_session` / `pick_port_and_start` 仍然是
   **静默**的（症状是"清理没跑""日志少了几行"这类指不回任何一个文件的现象）。

### Changed — 作业名从 `slurmate` 改成 `sj-<短名>`

`squeue` 的 NAME 列默认只有 8 个字符宽，`slurmate-code-server` 会被截成
`slurmate` —— 每个插件看起来都一样。现在按插件定名（`sj-code-server`），一眼看得出
是哪种服务。

顺带**删掉了模板里那行 `#SBATCH --job-name=slurmate`**：sbatch 的命令行选项
**覆盖**脚本里的 `#SBATCH`，而守护进程无条件传 `--job-name` —— 所以那一行
**从来没有生效过**。一个不生效的固定值，正是这个项目一路在清的那种假信号。

★ **已知退步，写在这里以便你需要时推翻它**：`sj-<短名>` 装不下会话的可辨认性。
同一个人同时开两个同插件的会话，`squeue` 里那两行**一模一样**。补偿是
`slurmate list` / `slurmate status` 给出会话 ID ↔ 作业 ID 的对应
（`docs/TROUBLESHOOTING.md` 第 1a 节补了这一步），要同时看两者用
`squeue -o "%.10i %.16j"`。另一条路是 `sj-<短名><session前4位>`。

★ 插件脚本的**内容**因此拿到与 `cluster/` 下那些文件**同一道**信任门（root 拥有 +
组/其他不可写）。理由：它们会被织进 root 拥有、由某个用户提交后以那个用户的身份
执行的作业脚本，某个普通用户能改写它，就等于让他的代码以**其他用户**的身份运行。
**这一条对"每一份"都成立** —— 每一份成品 root:root 0644，而 `jobs/` 目录本身
0755 root:root（用户要能**列**它、不能**写**它）。

### Changed — `BUILTIN_PLUGINS` 删掉，插件表改成**扫**出来的

```
<prefix>/share/slurmate/plugins/<任意目录名>/plugin.json
   ├─→ deploy.sh 读它 + job/start.sh ──→ 为它织一份 jobs/<ULID>.sbatch
   └─→ 守护进程启动时扫它 ────────────→ PluginSpec 表
```

守护进程里现在**一个插件名都没有** —— 连注释里都没有。

- 坏掉的插件目录**跳过并报出来**（点名哪个目录缺什么），**绝不让守护进程起不
  起来** —— 一个插件坏了不该带走整个站点。但它在配置里的块会因此变成"未知的插件"，
  那条仍然是硬错误。
- 短名撞车时**两个都不加载**：挑一个的后果是"哪个生效"取决于目录名的字典序。

### Changed — 清单（`plugin.json`）成为两侧共用的一份 schema

新增 `site.defaultCpus` / `defaultMem` / `defaultEnabled` / `bin` / `enumKeys`；
`contributes.submitPubkey` 升级为**两侧都读**。三处与计划不同：

- **`site.defaultEnabled`（缺省 false）取代代码里的 `DEFAULT_ENABLED_PLUGINS`。**
  为 true 的只有一种插件：**它就是升级前那个唯一可用的服务**。那条标记存在的全部
  理由是「升级不改变现有站点的行为」，而不是"这个插件比较重要"。
  于是老配置（一个 `[plugin:*]` 块都没有）仍然只开 code-server，**升级不改变任何
  现有站点的行为**（这一条有对应用例）。
- **去掉了计划里的 `blockKeys`。** 取值受限的键**就是**额外键。两份清单可以互相
  矛盾（一个键在 `enumKeys` 里却不在 `blockKeys` 里，表现是"校验规则写了，但用户
  永远写不进去"），一份不能。
- **`bin.env` 必须以 `SLURMATE_` 开头。** 那个名字会被塞进作业的环境变量表，前缀是
  本系统的领地；不限制的话，一个插件能覆盖掉宿主自己读的变量。

### Changed — 三处被刻意堵死的地方松绑了

| 位置 | 以前 | 现在 |
|---|---|---|
| `validate()` | 零插件 = **启动错误**（守护进程 100% 起不来） | **合法状态**。`--check` 说出来并给出安装目录的路径。以前那样等于把"框架"和"插件"绑死，而按设计基座不该知道有没有插件 |
| `op_submit` 的缺省服务 | 硬编码的 `code-server` | 全局键 **`default_plugin`**。没配就**要求显式给** `service_kind`（新错误码 `2 missing_service_kind`） |
| 新增 `--check-plugins` | — | deploy.sh 用它校验插件清单，**规则只有一份**，不在部署脚本里另写一套 |
| 插件**没有** `job/start.sh` | `deploy.sh` **中止部署**（而 `plugins/README.md` 与 `docs/ARCHITECTURE.md` 一直写着"可以没有"） | **合法状态**：装得上、看得见、**提交不了** |

★ 缺省**不选**"表里唯一那个"：隐式缺省会让**装一个插件 / 卸一个插件**这种配置之外
的动作悄悄改变行为 —— 今天提交成功的那条命令，明天可能落到另一个服务上。
`service_kind` 这一族因此有**四种**错误，分开它们的不是严重程度，是**"这件事该谁去
做"**：没给而没配缺省（补参数）/ 本站没装这个名字（升级客户端）/ 装了但没开
（找管理员开关）/ 装了但没有作业侧实现（**找管理员重新部署** —— 开开关没用）。

★ 「没有作业侧」这条路要说得出话，它一共有**六个**落点：`deploy.sh` 的 `warn` +
跳过、`--check-plugins` 的 `⚠`、`op_submit` 的 `4 service_kind_no_job`、
`op_plugins` 的 `can_submit`、`--check` 打印的逐插件作业脚本路径、以及
`plugins/README.md` 的说明。**它刻意没有进 `validate()`** —— 那里面没有警告通道，
加一条就是「一个这样的插件让整个站点起不来」，而那会连停掉正在跑的会话都做不到。
它照 `plugin_problems` 的现成先例处理：`--check` 打印 `⚠`、启动时记一条
`log.error`，但不拦启动。

### Changed — 客户端：池成为真正的安装点，界面照实说话

- **装与卸成对**（`plugins/install.js` 的 `installFrom` / `uninstall`）。
  **同一个 `(id, 版本)` 而内容不同 → 拒绝安装，不是覆盖** —— 那正是池模型唯一的
  危险处（静默地跑了另一个插件的代码）。
- **加载器扫两层**（`<池>/任意名/plugin.json` 或 `<池>/任意名/任意名/plugin.json`），
  两层的目录名都不参与判定。于是「用户拷一个目录进去」和「同一个插件的多个版本
  并存」两种布局能混着用。
- **演示后端改成通用的假网页服务**：页面路径、登录路径、字段名、cookie 名全部
  取自当前会话那个插件的清单。它以前叫"假 code-server"，那意味着基座里躺着一份
  code-server 的实现假设。
- **零插件时那一屏不再是死胡同**：说清池在哪，给「从目录安装…」与「重新扫描」和
  「打开插件目录」三个入口。以前那里写着"安装包可能不完整"。
- **删掉了插件块上那句「站点分发」标签。** 基座不再自带插件之后，池是唯一的来源，
  于是那个判定**恒为真** —— 用户自己从本地目录装进去的插件也会被贴上"站点分发"。
  一个永远显示、并且永远说错的标签，比没有标签更糟。（有对应用例钉着。）
- **`login.js` 换成 `weblogin.js`**：留下来的是「POST 一个表单、然后查 cookie」这个
  **通用机制**；`/login` + `password` + `code-server-session` 三个**具体值**进了
  清单。基座从此一个字都不知道 code-server 是什么，而任何 web 插件都能自动登录。

### Fixed — ★ 一条被新用例挖出来的既有缺陷：NFS 日志补写的水位错位

宿主把日志双写到节点本地与 NFS，作业结束时补写"尚未落 NFS 的那部分"。那个水位
记的是"**`log()` 调过几次**"，而服务进程的输出是**直接重定向到本地日志**的 ——
它插在两次 `log()` 中间，水位因此**错位**。症状：**NFS 日志末尾重复若干行**，
其中包括已经写过的"清理完成"，看起来像清理跑了两次。

修法两条，缺一不可：

- 水位改成记 **`LOCAL_LOG` 的行号**（`wc -l`），不是 `log()` 的次数；
- 服务进程的输出**写它自己的文件**，由 `cleanup_<短名>` **在宿主最后一行之后**并
  进来 —— 那个位置天然落在水位之后、会被补写带上去。

顺带把 `cleanup_<短名>` 的调用位置（在 `log "清理完成"` **之后**、NFS 补写
**之前**）写进契约：挪到前面，插件并进来的日志会落在水位之前，于是**静默地进不了
NFS** —— 而"认证被拒"这类事实只有服务自己知道。

### Added — `docs/KNOWN-ISSUES.md`：未修的缺陷有了一个账本

这一版把此前散落在对话里的「发现但没修」「从没实测过」「想清楚了但还没做」全部
落成一份文件。里面每条写了**位置、后果、修法**，编号（`F12` 之类）是稳定标识，
源码注释按编号引用。

已核实的缺陷（都**还没修**）：`goodbye` 回 `ok:true` 而 `scancel` 的返回值被丢弃；
`phase_release` 从不确认作业真的停了（叠加前一条 → **作业占着节点跑到 TimeLimit，
守护进程却报告"已释放"**）；提交配额拦不住并发的第二个提交；插件清单里没有
`defaultGpus`。

★ 一条离开这份文件的唯一方式是：**修掉它，并附上一条能真的红的用例。**

### 测试

集群侧 253 → **341**，客户端 171 → **175**，全绿。新增的用例都对应一条**具体的
静默失效**，而不是"覆盖率涨了"：

- 第 19 节：扫出来的插件表（坏目录跳过但**点名**、短名撞车**两个都不收**、零插件
  合法）、清单形状的十条错法、缺省插件**跟着配置走**、零插件时提交被明确拦住、
  以及一个守护进程**从没听说过**的合成插件走完整个提交路径。
- 第 21 节：宿主与插件之间只有一条接口（函数名）。真的**逐插件**织一遍、真的调一次
  分派，三种结果分得开（有钩子 / 没有钩子 / 钩子失败了）。
- 第 22 节：**真的把编织出来的作业脚本跑起来**（假 HOME、桩 `scontrol`）。
  ★ 这一节是作业模板在仓库里**唯一**的执行防线 —— 又只能在计算节点上跑。
  （`run.sbatch` 此前还因为**没有 `.sh` 后缀**而不在 CI 的语法扫描清单里，
  一并修掉了：那条 `git ls-files` 加上了 `'*.sbatch'`。）
- 第 17 节新增：对**每一个**真插件，`build_sbatch_argv` 的末项就是**它自己**那一份
  `<ULID>.sbatch` —— 堵的是"提交了 code-server、跑起来的是 sshd"这个此前在全仓库
  **零覆盖**的失败模式。
- 客户端那条「不许按 `source` 给插件贴来源标签」：文件是**静态文本比对**，
  且**先剥掉注释**再匹配（不剥的话，解释这条禁令的那段注释自己会让检查红）。
- 客户端新增三条：站点装了但没有作业侧实现（按钮灰、话说得出来）／
  老守护进程不报 `can_submit` 时**缺席 ≠ 否**（两种缺席都测）／
  panel.js 里那四句"起不来的原因"**互不相同**（本机跑不起渲染层，只做文本比对）。

★ **变异验证**：改坏一处，对应用例必须红。除上一批 13 条之外，这一版又做了四条：
把顶层语句那一段落回"所有插件共处一份"（隔离用例红，对照用例仍绿）／
`runnable` 忘算 `canSubmit`（灰按钮那条红）／三态默认反过来（缺席当否那条红）／
把两句话改成一模一样（四句话那条红）。**每条变异只有它自己那条用例变红。**

### Changed — 文档不再写行号
`文件:行号` 形式的引用**机制性地废除**了。这一版清掉了文档里 198 处行号引用 ——
它们中的**绝大多数**在插件剥离之后已经指错地方（`cluster/slurmate-sessiond`
整体下移一千多行），而**失效的行号比没有行号更危险**：读者会翻到一个看起来像那么
回事的地方，然后按错误的前提理解代码。

现在一律按「文件名 + 符号名」指路。约定写在
[CONTRIBUTING.md](CONTRIBUTING.md)〈文档〉一节。

### 尚未做（下一阶段）

> ⚠️ **这一节描述的是 0.5.0 当时的缺口。** 其中前两条已经被 0.6 做掉了 —
> 见上面那一节。留在这里是因为变更日志是**历史**：删掉它等于抹掉"当时是什么样"。
> 只有一条要更正：0.5.0 说"`installFrom(dir)` 就是将来那条路要走的同一个函数"，
> 而**分发最后没有走它**（语义相反：一个是"用户自己装"，一个是"站点说了算"）。
> 见 0.6 的〈Changed〉。

- **站点分发插件**还没有（没有取插件文件的 op）。本版做的是**安装点**，而
  `installFrom(dir)` 就是将来那条路要走的**同一个函数**。
- **签名 / 钉公钥 / 逐插件同意 / 进程隔离**四条护栏，是代码分发那一层的前提。
  今天它们**还不是前提**，因为唯一的路是「用户自己放进去的东西」；**一旦有一条从
  远端来的路，它们就是前提**。
- **`deploy.sh` 整条没有在真机上跑过**（需要 root + 一台控制节点）。本版用等价方式
  验过其中每一段，但整条路仍要上真机。
- 插件的测试仍然住在 `client/test/` 里 —— 一个独立项目应该有自己的测试项目。
- 其余未修项、未实测项与结构性欠账，见 [docs/KNOWN-ISSUES.md](docs/KNOWN-ISSUES.md)。

---

## [0.4.0] — 未发布（被 0.5.0 取代，从未部署）

> 这一节的下面那一节（0.3.0）同样从未部署过。**从 0.2.0 升上来的站点直接看
> 0.5.0 那一节就行。**
>
> ⚠️ 这一节里凡是提到 `client/src/main/plugins/<名字>/` 的地方，都描述的是一个
> **从未发布过的中间形态** —— 0.5.0 把两个插件搬去了仓库顶层的 `plugins/`。

### Changed — 插件的身份是**铸造**出来的，不是起名起出来的

一个插件现在有三样东西，各司其职，**不要把它们的职责合并**：

| | 是什么 | 可变吗 | 出现在哪 |
|---|---|---|---|
| `id` | ULID，诞生时铸一次，**全球唯一** | **永不可变** | 客户端的解析键 |
| `version` | `x.y.z` | 每次改动都变 | 客户端的解析键 |
| `name` | **站点内**的短名（`[plugin:<name>]` 的块名） | 可以改 | 配置块、日志、`service_kind` |

为什么要有 `id`：插件由**站点**分发，没有市场能在线升级，而站点更新频繁、也可能
长期不更新。要靠名字认出"这是同一个东西"是做不到的 —— 两个站点各写一个 `jupyter`
指的是不同的东西，而同一个插件被两个站点分发时又该被认出来。铸造出来的 id 才答得
了这两个问题。

于是客户端那边有了一个**池**（`~/.slurmate/plugins/`）：任意站点的安装都丢进这个
池，任意站点的使用都从这个池里取，卸载就是把它拿掉。

- **同一个插件被两个站点分发** → 两边 `id` 相同 → 池里合并成一条，只多记一个来源。
- **两个站点各写一个 jupyter** → 两个不同的 `id` → **并存**，各自标明来源。站点
  更新频繁也好、拒绝更新也好，都不会把对方挤掉。
- **同 `(id, 版本)` 而内容不同** → **两个都不加载**并报错。这是池模型唯一的危险处：
  按站点分目录时取错最多浪费几十 KB，池里取错就是**静默地跑了另一个插件的代码**。
  宁可暂时不可用 —— 那种失败是看得见的（会话变成"未知服务"，仍然接得上隧道、
  停得掉）。这条防**意外**，不防**恶意**；防恶意靠站点签名（见「尚未做」）。

### Changed — 会话带上解析键，而且**必须是提交那一刻的那一版**

会话视图多了一个字段 `service_plugin` = `"<id>@<版本>"`，由守护进程在提交时从它
自己的插件表里抄下来。

**为什么不能读的时候现算**：`op_plugins` 报的是站点**当前**的清单，而一个跑着的
作业用的是它**提交时**那一份代码 —— 作业侧与客户端侧是配套的两半，站点一升级插件，
按"当前清单"解析已跑的会话就会把**新版本**的客户端代码接到**旧版本**的作业实现上。
`service_plugin` 是 NULL 有确定的含义（会话是从 nft 规则恢复出来的，服务端也不知道
它是什么），与"字段不存在"（老守护进程）是两回事。

同理，**客户端在会话创建时把插件对象捕获一次**，此后所有状态变化都用它、不再查表。
于是站点升级插件对正在跑的会话**完全没有影响**，把一个插件从池里卸掉也不影响它。

### Added — 插件是一个目录，清单驱动

```
<插件目录>/
  plugin.json        身份、版本，以及框架会读的那几条声明
  client/index.js    客户端侧代码 —— **可有可无**，没有它就是纯声明式插件
```

这一版把两个插件改成这个形状时，它们当时还放在
`client/src/main/plugins/<名字>/`（= **基座内部**）—— 0.5.0 把它们搬去了仓库顶层的
[`plugins/`](plugins/)。这一节记的是那一版的设计，路径那部分请看 0.5.0。

与站点分发的插件**走同一条加载路径** —— 后者只是多看一个目录，不是一套新机制。

`plugin.json` 里框架真正会读的是 `contributes`：

```json
{ "surface": { "kind": "web", "path": "/" },   // 有界面，加载 origin + path
  "layout": true,                               // 要一个布局组
  "submitPubkey": true,                         // 提交时要带一行公钥
  "legacyDefault": true }                       // 老守护进程的会话兜到它
```

★ **目录名不参与任何判定**，身份来自清单里的 `id`。目录名纯粹是给人看的（仓库里那
两个用可读名字，池里装的用 `<id>@<版本>`），所以仓库里一眼能看出哪个是哪个，而
加载器只有一条逻辑。

清单里的**未知键报错而不是忽略**（沿用 0.2.0 定下的规矩）；`engines.slurmate`
不满足就**不装**，而不是装上之后在某个角落炸。

### Changed — `windows.js` 里没有 IDE 了

`showCodeServer` / `codeSession` / `codePartition` / `codeOrigin` / `hasCodeView` /
`hideCodeView` / `reloadCodeServer` 全部换成通用的 `showSurface({url, partition})`
一族。那个文件现在只知道"有一块原生视图盖在面板上，它加载某个 URL，属于某个存储
分区" —— URL 后面是 VS Code、是 Jupyter、还是别的什么，它一概不知道。

**开界面这件事由框架按 `contributes.surface` 做，不由插件代码做。** 这条边界让一个
**没有客户端代码**的声明式插件照样能开界面：开界面本来就不需要代码。

### Changed — 界面按插件画块

`panel.html` 里那个写死的「开始开发」按钮没有了，改成从 `pluginsView()` 画出来
的 N 个块 —— 每个块显示标题、短名、版本、来源、站点开关、本机开关、默认资源。
理由很直接：写死一个按钮的话，「站点卸掉一个插件」在界面上就变成了"点了报错"，
而不是"那一块不见了"。

- **本机开关按 `id` 记**（`config.json` 的 `plugins` 段），不按短名：池是全局的，
  两个站点可以各有一个叫 `jupyter` 的插件而它们是两个不同的东西。
- 池里的问题（撞车 / 坏清单 / 引擎不兼容）与「站点有而本机没有的插件」各自成块
  报出来 —— 前者要做的是"删掉多余那一份"，后者是"升级客户端"，两件事不能糊成
  一句"不可用"。
- **「本地地址」那一行不再无条件写 `http://…`**：那是按插件声明的 `surface` 决定
  的。对中转站会话写 URL 是一句假话（那个端口后面是 SSH），用户照着打开只会得到
  空白页，而他会以为是客户端坏了。
- 高级选项里那些「默认 2」「默认 8G」的占位符去掉了 —— 默认资源现在按插件不同，
  写一个数字就是在替另一个插件说话。

### 尚未做（下一阶段）

> 三条都**仍然成立**（0.5.0 也没有做），措辞按 0.5.0 的状态更新过了。
> 权威账本是 [docs/KNOWN-ISSUES.md](docs/KNOWN-ISSUES.md)。

- **站点分发插件**（声明式与代码分发）还没实现。客户端已经会扫
  `~/.slurmate/plugins/` 那个池、**也能往里装**了（0.5.0 做了安装点），但
  **没有任何东西会从集群取插件文件** —— `op_plugins` 还没有那个 op。
- **站点签名 / TOFU 钉公钥 / 逐插件同意 / 进程隔离**四条护栏，是代码分发那一层的
  前提，同样尚未做。
- sshd 会话仍然**本机验不了**：要部署到控制节点后手工提交一个
  `service_kind=sshd` 的会话，从登录节点 `ssh -W` 连一次。

---

## [0.3.0] — 未发布（被 0.4.0 取代，从未部署）

> 三处版本号（`client/package.json`、`cluster/slurmate`、`cluster/slurmate-sessiond`）
> 现在都是 **0.3.0**。**两端协议不兼容，必须一起升。**

### Changed — 「服务种类」变成「插件」，站点可增删

`code-server` 与 `sshd` 从两个硬编码的特例变成**插件**：两侧各有一半实现，站点
在配置里用 `[plugin:<名字>]` 块决定装不装、开不开、默认给多少资源。

**配置格式变了**（`slurmate.conf`）：

```ini
# 站点通用键（必须写在所有块之前）
cluster_cidr = 192.0.2.0/24
...

[plugin:code-server]
enabled      = yes
default_cpus = 2
default_mem  = 8G
bin          =
auth_mode    = password

[plugin:sshd]
enabled      = no
default_cpus = 1
default_mem  = 2G
bin          =
```

- **`service_kinds` 这一行没有了**（它是 0.2.0 里刚加、从未部署过的键，所以没有
  迁移负担）：「站点开了哪些插件」现在由**有没有那个块 + `enabled`** 表达。
- **`code_server_bin` / `sshd_bin` / `auth_mode` 从顶层搬进各自的块。**
- **一个 `[plugin:*]` 块都没有的老配置仍然只开 code-server** —— 与升级前完全一致，
  这一条有专门的用例钉住，它是升级能否平滑的支点。
- 写一个 `[plugin:sshd]` 块**不会**顺手把 code-server 关掉，也**不会**因为"块在那儿"
  就自动开启 sshd：想开必须写 `enabled = yes`。让开启是一条看得见的决定。
- 默认资源从两个代码常量变成**按插件**的一项。在 IDE 里跑语言服务器和在 shell 里
  跑 codex 不是一回事。

**协议变了**：新增 `plugins` op；`partitions` 的响应**删掉 `defaults`**（默认资源
只能有一个来源）；`submit` 收 `service_kind` 与 `ssh_pubkey`。

### Added — ★ 插件增减不许把客户端带崩

这是这次改动的验收标准，四条不变量各有用例：

1. **会话的状态 / 心跳 / 停止永不看插件**（只认 `session_id`）—— 这是「卸载插件之后
   用户仍然能停掉作业」的全部依据；
2. 服务端不认识的插件名 → 提交被**明确拒绝**，不是起一个连不上的作业；
3. 客户端不认识的插件 → **只解释、不动作**，但隧道**接起来**（那是用户唯一的出路）；
4. 客户端插件目录里的坏文件 → 跳过、记一条、其余照常。

### Added — 客户端插件注册表

`client/src/main/plugins/` 一个插件一个模块文件，注册表**扫描目录**而不是手写一张表。
文件名就是插件的身份（会话里的 `service_kind` 就是它），导出里的 `name` 与文件名不
一致时宁可跳过 —— 那种不一致会让"改了 A 文件、生效的是 B"永远说不清。

框架（`index.js` / `session.js` / `windows.js`）**不认识任何插件名**：判据一律是
"有没有布局组""两个开关开没开"这类**框架的事实**，不是"是哪个插件"。

### Added — 本机插件开关

`config.json` 的 `plugins` 段记「本机要不要这个插件」，与服务端的 `enabled` 是
**两件事**：站点决定允不允许，用户决定自己要不要。两者都开，界面上才会出现那个
按钮。认不出的值按「跟着站点走」处理，不读成「关掉」—— 一个笔误不该让功能凭空消失。

### Fixed — 会话结束后用户出不来（既有缺陷）

code-server 视图是**原生层**，覆在面板上方，而没有**任何地方**销毁它。会话结束、
隧道已停、页面已经打不开，那块死页面还盖在面板上，而面板上正是「重新开始」那几个
按钮 —— 用户唯一的出路是关掉客户端重开。现在在 `RELEASING/ENDED/ERROR/IDLE` 时
收起它。**`RELEASING` 是关键那一档**：`stop()` 一发出状态就是 releasing，而它要等
下一次 status 轮询（60 秒）才可能变 ended。

### Fixed — `slurmate submit` 起不了中转站会话

`cmd_submit` 从来没有 `--service-kind`，于是「提交一个 sshd 会话」这条路只能靠手写
`rpc` 的 JSON。现在有了 `--service-kind` 与 `--ssh-pubkey`，另加 `slurmate plugins`
列出本站的插件与各自的缺省资源。

### 已知未验证

- `run.sbatch` 的 sshd 分支**本机验不了**：需要部署到控制节点后手工提交一个
  `service_kind=sshd` 的会话，从登录节点 `ssh -W` 连一次。
- 界面对 `service_kind` 仍然**零感知**（按约定，界面在插件结构定型后统一做）。

---

## 布局组 — 未发布，而它**没有自己的版本号**

> ★ **这一节的标题从前是 `## [未发布] — 布局组`，那是一个排版事故。** 本文件所有
> 方括号里的东西都是**版本号**，而「布局组」不是 —— 于是这一节读起来像"发布过一个
> 叫布局组的版本"。它实际是：这批改动落在 `0.2.0` **发布之后**、`0.3.0` 的版本号
> 写下来**之前**，所以它按时间排在这里（新到旧），并且随 `0.3.0` 那条线一起发出去。
> Keep a Changelog 里 `[未发布]` 是留给**下一个**版本的，一个功能组不该占那个位置。

### Added — 布局组：一个「布局」可以被多条连接共用

**不是破坏性变更。** 迁移沿用旧的分区名（`persist:slot-1`）与旧端口，老用户的
编辑器布局、最近打开的文件、登录状态原样保留 —— 升级后第一次打开不会重置任何
东西。RPC 协议无变化，集群侧一行未动。

在此之前所有连接共用一份布局（`slot` 号硬编码为 1）—— 那是写死的结果，不是设计
出来的能力。现在它是一个可控的、看得见的东西：

- **一个布局组 = 一个本地监听端口 = 一份编辑器布局。** 若干条连接可以共用一个组
  （多对一）；没有任何连接在用的组会被自动回收。
- 三处入口：每条连接行里的下拉、「连接与布局」的两列映射图、以及状态条里的
  选择器（会话跑起来之后窗口主体被 code-server 页面盖住，那是唯一还够得着的
  入口）。三处共用同一份由主进程推导好的数据，界面不做任何推导。
- 组的名字可以改。名字只是给人看的 —— 身份永远是 id，它决定存储分区，**永不复用**。
- **切换布局在运行中立刻生效**：换本地端口、重连隧道，**集群上的作业一动不动**。
  这一条有测试钉住：切换全程 `backend.rpc` 调用计数增量为 0。
- **切走一个只被自己用着的布局会把它删掉**，所以下拉里那一条会写明「只有这一条
  连接在用 —— 切走就会被丢弃」，真切走时还要再确认一次；有会话在跑时，确认文案
  里会说明**页面会重新加载、未保存的编辑内容会丢失** —— 这比「布局变了」严重得多，
  用户有权在按下去之前知道。判定权在**主进程**：界面手里那份引用计数随时可能已经
  陈旧，所以由主进程回 `would_discard`，界面拿它的原话去问用户。

**存储分区按组 id 命名，不按端口。** 组被回收后端口会被下一个组复用；按端口命名的
话，新建的「空白布局」会继承上一个组的 localStorage 和登录 cookie（Chromium 的
cookie 按 host 存，端口根本不参与匹配）。按 id 命名后，新 id → 新目录 → 天然空白，
与端口多少无关。

### Fixed

- **「断开」之后再开会话，会一直报「会话已在进行中」，直到重启客户端。**
  `SessionController.stop()` 置为 `releasing` 之后把状态轮询也停掉了，于是它再也
  走不出去；而复用 controller 的判断只认 `ended`/`error`/`idle` 三个终态。
  现在 `releasing` 也算终态 —— 旧会话已在拆除，新会话该拿一个新 controller。
  若旧作业还没被守护进程收掉，用户拿到的会是服务端那句准确的「已有 1 个活跃会话」。
- **隧道重建时丢掉了 `Tunnel.start()` 的返回值**：`snapshot().localPort` 会指向一个
  没人监听的端口（界面上的「本地地址」是死的），顺移结果也不写回配置。
- **会话结束（`ended` / `session_gone`）时不释放本地监听端口**：浏览器连上去会被
  接到一个不存在的目标，表现是「页面打不开但也不报错」。
- **端口顺移不再落进另一个布局组占着的端口**：落上去的后果不是「换了个端口」，而是
  两个组在配置里声称同一个端口，每次启动谁先绑谁赢，布局在两个 `origin` 之间反复
  横跳 —— 而界面一切正常。
- **拆掉 code-server 视图时不再报假的「页面崩溃了」。** 换布局要销毁重建视图
  （`partition` 只在 `new WebContentsView` 的构造参数里读一次），而那会触发
  `render-process-gone` —— 不区分的话，用户每切一次布局就会看到一条假警报。

## [0.2.0] - 2026-09-14

**破坏性：RPC 协议 v0.2，两端必须一起升。** 三处版本号（`client/package.json`、
`cluster/slurmate`、`cluster/slurmate-sessiond`）现在都是 0.2.0。
v0.1 的客户端连不上 v0.2 的守护进程，反之亦然。

### Changed — **破坏性：RPC 协议 v0.2**（两端的版本必须一起升）

- **删掉「用途」这一层。** 配置里不再有 `[purpose:*]`；`purposes` op 改成
  `partitions`；`submit` 直接收 `cpus`/`mem`/`gpus`/`partition`/`time`，全部可选。
  理由：「用途 → 分区」是**策略**，而 Slurm 已经知道**事实**。多抄一份就多一处
  会与实际分叉、且分叉了没人会发现的副本。详见 [docs/PROTOCOL.md](docs/PROTOCOL.md)。
- **缺省由服务端填**（2 核 / 8G / 从有权限的分区里随机挑一个）。客户端不自己编
  默认值 —— 否则一个改过的客户端省略字段就能要到整机。
- **客户端改用纯公钥认证，不再接受密码**，也不再读 `~/.ssh`。密钥由客户端生成并
  自托管，公钥在界面上可查可复制，用户注册一次。
- 客户端不再实现任何身份管理（改密码、设邮箱、查账户）。它从「账户已经配好了」
  开始 —— 别的集群未必用同一套身份体系。
- **用户主动的终止一律是彻底终止。** 「断开连接」「结束会话」「关闭窗口」现在都走
  `goodbye`：取消作业、释放资源。此前「关闭窗口但保持作业运行」是一条能被点击
  触发的「不释放」路径 —— 而它本该服务的场景（断电、睡眠、网线被拔）里，客户端
  根本执行不到那段代码；能被它命中的只有用户明确表达了终止意图的场合。保活留给
  守护进程的 `suspect`(300s)/`orphaned`(1800s) 容错窗口，那条路客户端说不上话。
  关窗前的确认弹窗保留（只为拦误点 ×），但**只在真有会话在跑时**才弹，默认停在「取消」。
- **RPC 命令显式走 `/bin/bash -c`**，不再交给登录 shell 定解释器。sshd 执行 exec
  请求用 `$SHELL -c`，而 HPC 登录节点上 `$SHELL` 常是 zsh，与 bash 并非完全互通 ——
  写死的命令串一旦沾上某种方言，就会变成「这台机器能连、换一台就认证失败」。
  注意它**挡不住 rc 文件**（sshd 仍先用登录 shell 解析，zsh 的 `~/.zshenv` 连 `-c`
  都会读），rc 噪声由应答解析兜底：从后往前找第一个带布尔 `ok` 的 JSON 对象，
  而不是假定应答在最后一行；解析不出来时把原始输出一并报出来。
- **「登录节点」这一屏重排**：表面只放**已保存的连接**，公钥与地址表单折进
  「新建连接…」里（一条都没有时自动展开）。公钥和地址是一次性的活，而用户每次
  打开客户端真正要做的事是「连上上次那台」。
- **私钥不再有「保存方式」这个选项**，永远加密保存。此前那个下拉框把
  「你的私钥会以明文躺在磁盘上」包装成一个需要用户自己权衡的选项 ——
  选明文的那个用户并不知道自己在放弃什么，选加密的那个也不该为此感到庆幸。
  机器上没有凭据库时，界面如实说明「存不下来、下次启动会换一把新的」，而不是
  再给一条明文退路。旧版本写下的明文文件**读得出来**（读不出来会让用户以为密钥
  丢了、跑去重新注册），读到后自动加密重存。

### Changed — 密钥改为**每条连接一把**

- **密钥的作用域从「客户端」改成「连接」。** 公钥是注册在**某个账户**上的，而一条连接
  就是「哪个账户、哪台机器、哪个端口」—— 按连接存，重新生成一把钥匙才只作废那一条，
  而不会顺带把别的连接打断。
  - 「新建连接」时**密钥当场生成**（早于用户填地址）：他得先把公钥注册到 IDM、回来才连得上，
    否则第一次「保存并连接」注定认证失败。这把「还没有归属」的密钥有自己的一格，
    并且**落盘、跨重启保留** —— 用户可能复制完公钥去注册、中途关掉客户端再回来，
    丢掉它等于让刚注册的那把公钥当场作废。
  - 每条连接在列表里带一个**「编辑」**入口：改地址，以及看/复制/重新生成**它自己的**公钥。
  - **删除连接会连带删掉它的私钥**（此后是一个带确认的不可逆操作）。
  - 旧版本（schema ≤ 3）那份**全局**密钥在升级时原样复制给每一条已有连接 ——
    那正是升级前的事实，复制完每条的行为都不变，用户也不必重新去 IDM 注册一遍。
    一条连接都没有时它被留在原地，等有了第一条再搬（删掉它等于让已注册的公钥凭空消失）。
  - 编辑时把地址改成另一条已有的连接会被**拒绝**并指出撞上的是哪一条：两条同身份、
    各带一把密钥的条目，界面上完全看不出差别。
- **`app:bootstrap` 不再返回公钥。** 界面上没有「全局密钥」这个概念了，表单打开时
  单独去问（`app:publicKey` / `app:newKey`）。顺带消掉一条老毛病：`renderKey` 此前
  读的是 `boot.fingerprint` 而 bootstrap 给的字段叫 `keyFingerprint`，于是指纹在首屏
  从来没显示过、「私钥存不下来」的提示也因为 `undefined === false` 不成立而永远不出现。

### Fixed

- **「新建连接」的地址栏预填了上一条连接的地址。** 用户不改直接点「保存并连接」，
  得到的只是「又存了一条一样的」，而他以为自己新建了一条。现在一律空白，端口也不给
  默认值 —— 编一个默认端口出来，用户会以为「填不填都行」，而错端口的表现是连接超时。
- **保存之后表单会自己弹回来。** 连不上时把「新建连接」又摆一遍，看起来像什么都没存进去
  （而那条连接其实已经在列表里了）。现在无论连上没连上，表单都收起来 ——
  列表里那条带着「连接」和「编辑」，连不上时日志里那条错误会说清下一步做什么。
- **公钥框里看不出钥匙换了。** 那个只读文本框显示不出任何信息：所有 ed25519 公钥的
  前 40 个字符逐字相同（`ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI` 是固定的），
  于是「换了一把钥匙」在框里看起来毫无变化 —— 而这是最要命的一种变化，
  用户会以为还是原来那把，实际拿去认证的已经不是了，症状只有「认证失败」。
  现在**不显示公钥本身**，只显示**指纹**（短、每一位都随机、拿去和 IDM 里那条一比对
  就知道对不对），公钥交给「复制公钥」按钮。密钥注释也从只有日期改成**精确到分钟**：
  只到天的话，同一天重新生成的两把钥匙在 IDM 的列表里名字完全一样。
- **「这台机器存不下私钥」只是一行灰字。** 它预告的正是后面那个「认证失败」：
  密钥存不下来 → 下次启动换一把 → 已经注册到 IDM 的那把公钥作废。现在它是一块告警框，
  并把后果写全。
- **连接条目没有备注可读。** 表单里加了「备注（可留空）」一栏；列表里**有备注就显示备注**
  （用户起名就是为了不必再读地址），没起名才回落成 `用户@主机:端口`。两者不并排显示 ——
  并排等于把备注降级成一个前缀，那这个名字就白起了。地址仍在 `title` 里，鼠标停一下就有。
- **「新建连接」「探测全部」摆在列表下面**，条目一多就得先滚过全部条目才够得着。
  现在在区块标题行右侧。
- **连接列表没有自己的滚动**，条目多时会把日志顶出视野。现在列表与日志各自滚。
- **按住 Ctrl 会把日志刷满「已放行：Ctrl+Control」。** 两处叠加：操作系统对按住的
  修饰键会**连续**产生 keyDown，而每一次的 `key` 都是 `Control`。现在修饰键单独按下
  不算一条消息（它既没有被吞掉的可能，也不是一个动作），自动重复的事件也不再重复上报。
  并且把**放行**类诊断整个收进演示模式 —— 它的用途是「验证按键到底有没有直达页面」，
  那件事只在拿演示后端做对照时才需要看；真实模式里用户是在干活，不是在校验外壳。
  被我们**吞掉**的键（F12 之类）仍然照报：那是在解释「为什么按了没反应」。
- **`All configured authentication methods failed` 原样透给用户。** 这是 ssh2 的原话，
  唯一含义是**服务器不认这把公钥**，但它一个字都不提用的是哪把 —— 于是用户唯一能
  做出的反应是反复重试。现在这条错误里带着账户、指纹、公钥原文和一条能自己跑的
  核对命令（`ssh-keygen -lf`），并列出三种修法不同的可能（没注册成功 / 注册的是另一把 /
  那个端口不允许纯公钥认证）。这句话本身没有任何编码问题：本机核过，界面显示的公钥行
  与 ssh2 真正拿去认证的公钥 blob **逐字节相同**。
- **日志区没有高度上限**，会一直往下长，把上面的表单顶出屏幕 —— 而上面那块才是用户
  正在操作的东西。现在日志自己滚（`max-height` + 滚动，`min-height` 兜住空态，
  否则一条都没有时整块会塌成 0，看起来像界面坏了）。
- **`upsertConnection` 在编辑场景会造出两条同身份的条目**：输入的 id 指向 A、地址却撞上 B 时，
  它会照着 B 改 A 的字段。现在返回 `conflict`，由界面明确拒绝。
- **`panel.html` 的内联 `style` 属性被自己的 CSP 静默丢弃**（`style-src 'self'`
  没有 `'unsafe-inline'`）。布局一律改用 class。
- **`backend-ssh.js` 的私钥解析失败会抛异常穿出「不抛异常」的契约**，在真机上
  表现为主进程一个没人处理的 rejection。现在返回 `bad_private_key`。
- **同一台登录节点被反复保存成一串一模一样的条目。** 「保存并连接」每次都生成
  一个新的条目 id，而一条连接的身份其实是 `user@host:port` —— 不改任何字段连点
  几次，列表就越点越长，而用户分不清该点哪一条。现在按身份判重复用已有条目，
  读取配置时也顺手把旧版本攒下的重复条目合并掉。
- **界面启动了「已连接」这个状态，却没有反向的出口。** 现在每条连接带一个
  「断开」按钮，断的只是客户端这一跳（作业仍在集群上跑，再点「连接」会接回去）；
  会话进行中不允许断开，否则界面会显示「未连接」而资源还在烧。
- **首屏的密钥指纹从来没显示过**：`renderKey(boot)` 读的是 `info.fingerprint`，
  而 bootstrap 给的字段叫 `keyFingerprint`，于是它一直是 `undefined`。
  同一个原因还让「私钥存不下来」的提示永远不出现（`undefined === false` 不成立）。
- `check-sanitized.sh` 的「无私钥」规则把 **PEM 头文本本身**也算命中，
  而 `keys.js` 是编码器、必须写出那个常量。不是放宽规则，而是让它表达真正想
  表达的东西（私钥**材料**），覆盖三种真实泄漏形态；并新增
  `tools/self-test-sanitized.sh` **反向验证**（真的种进三种形态，断言检查会红）。

### Changed — 集群侧：配置从 INI 分节改成扁平单文件，只留 12 个键

**这一节的全部改动都在 `cluster/` 下，是为了实现上面的 v0.2 协议。**

- **配置格式：`[section]` + `key = value` → 扁平的 `键 = 值` 单文件。** 只有三种行：
  注释、空行、赋值；行尾 `# …` 也是注释。
- **能查到的不再配置。** 分区名、分区的时间上限、默认分区、用户的可用分区与账户 ——
  这些 Slurm 自己知道，写进配置就是第二份副本，而副本会在管理员改分区之后悄悄过期，
  分叉了没有任何东西会告诉你。相应地，`[purpose:*]` 整节被删除。
- **收起 20 多个「只有一种正确取值」的键**（`socket_path`、`state_dir`、`db_path`、
  `log_dir`、`tick_seconds`、`[lifecycle]` 阈值、`[renew]`、`[quota]`、`password_bytes`、
  `job_script`、`job_log_subdir` …），改成代码常量。前四条与 systemd 单元的
  `RuntimeDirectory`/`StateDirectory`/`LogsDirectory` 是同一个事实 —— 在配置里改掉它，
  单元不会跟着改，症状是状态目录凭空换了个地方、旧数据不见了，而报错里没有一个字
  提到配置。`job_script` 改为从守护进程**自身的安装位置**推导。
- **`max_time` 改为按分区的 `MaxTime` 动态决定**（取分区上限与硬上限 7 天的较小者）。
  原来那个配置项会在管理员调整分区后过期，而用户拿到的是「提交时才被 Slurm 拒绝」的
  错误 —— 与真正的原因隔着一层。
- **`slurmate` CLI 不再读配置文件。** socket 路径是编译期常量。此前 CLI 用一套手写的
  逐行扫描去解析与守护进程（`configparser`）**同一个文件**，两边对同一个文件得出不同
  结论是可能的，而症状只是「连不上守护进程」。
- **拼错的键、重复的键都是错误**，不再是静默忽略。静默忽略一个拼错的键，后果是
  「文件里写着，而实际什么也没发生」；这一条同时也守住了上面删掉的那些键 ——
  老配置里的 `max_active_per_user` 会被明确拒绝，而不是静静地失效。

### Fixed — 集群侧：三个已核实的缺陷（此前只记录、未修）

- **`cluster_cidr` 写窄会让全部 ACL 静默失效。** 基础规则 `ip daddr != CIDR accept`
  在链首、会话 drop 规则追加在链尾，所以 CIDR 漏掉某个节点时，那条流量会被链首
  放行，而策略是 `accept` —— 没有任何迹象。三件事一起做了：
  **①** 它**不再有默认值** —— 留空即拒绝启动（此前默认值恰好是文档占位网段
  `192.0.2.0/24`，也就是「漏配」等于「静默无保护」）；**②** 校验前缀长度（此前只查
  地址部分，于是 `192.0.2.0/99` 能通过自检而在 nft 里是语法错误）、拒绝 `0`、拒绝主机位
  不为 0 的写法；**③** 启动时与 `scontrol show node -o` 的 `NodeAddr` 全集交叉核对，
  **发现节点落在网段外就拒绝启动并逐个点名** —— 这是唯一能同时发现「网段写窄了」与
  「填的是文档示例网段」的地方。
  （取不到节点列表时**不阻塞启动**：那是控制器抖动，不是配置错了；因为一次查询失败
  就拒绝服务，会把一次瞬时抖动放大成一次停机。但那是本校验唯一的缺口，所以那种情况下
  日志会明确写出「本次没有核对」以及它意味着什么。）

  > 这里**刻意没有**「拒绝 RFC 5737 文档网段」那一条，尽管它看着能一眼抓住「照抄示例
  > 忘了改」。因为那会**拒绝一个完全合法的配置** —— 用 `192.0.2.0/24` 的测试集群是存在
  > 的，软件没有依据判定它不是真的。一个不许人填真实值的校验，是软件的错，不是配置的
  > 错；而它想防的事由 ③ 覆盖，报错还更精确（会点名是哪几个节点落在外面）。
- **配置里「留空则用 `shutil.which` 查找」是一句谎话。** 代码里只有
  `or "/usr/bin/sbatch"` 硬兜底 —— Slurm 装在 `/opt/slurm/bin` 的集群照抄示例配置
  **直接起不来**，报的还是「Slurm 命令不存在: /usr/bin/sbatch」，把人引向「是不是没装
  Slurm」。现在是真的查找：先 `PATH`，再依次查 `/usr/bin`、`/usr/local/bin`、
  `/opt/slurm/bin`、`/usr/sbin`、`/bin`、`/sbin`。`code_server_bin` 同理。
- **`validate()` 漏查 `squeue` 与 `sacctmgr`**：这两个可以是错路径而启动通过，运行时
  才失败 —— 那时报出来的是「查不到作业」或「没有账户」，与真正的原因（路径写错）
  隔了好几层。现在五个命令全查，且区分「配置留空、自动查找失败」与「显式指定的路径
  不存在」两种情况。

### Added — 集群侧

- **`partitions` op 与自适应提交。** 分区列表从 `scontrol show partition -o` 现查并与
  该用户的 association 求交，返回每个分区的 `MaxTime` 与是否默认。
  `submit` 的字段全部可选，缺省由服务端填（2 核 / 8G / 从有权限的分区里随机挑一个）。
  **随机挑而不是按顺序挑**：顺序挑会让所有默认会话落到同一个分区，而用户要的是分散。
- **服务的「缺省」路径在权限查不到时退化为不带 `-p`**，交给 Slurm 的 association 兜住，
  并在响应里带 warning。显式点名分区的路径仍然 fail-closed。这**不是漏判** ——
  改成拒绝会让一次 `sacctmgr` 抖动变成所有用户都开不了会话，而这里没有任何安全损失。
  代码里写明了缘由，免得后人误「修」。
- **`submit` 响应的 `warning` 字段**（时间被截断、内存参数无法识别、分区退化为 Slurm
  默认）。客户端已接进 `snapshot().warning` 并在界面上显示 —— 服务端替用户做了决定就
  必须说出来，否则他会以为自己要到了。协议里有这个字段的唯一理由就是被显示出来。
- **`build_sbatch_argv()` 拆成纯函数。** 这个命令行是「哪些 flag 在什么条件下**不出现**」
  的唯一实现（比如分区为空时省略 `-p`、没要 GPU 时**完全省略** `--gres` 而不是写
  `gpu:0`），而它的分支错误只有到真集群上、且只在某些分支上才暴露。拆出来之后每条
  分支都能在本机断言。
- **创建时发现数据库是 v0.1 的会明确拒绝**，并给出修法（`rm -f claims.db`，附上为什么
  删它是安全的）。`sessions.purpose` 那一列是 `NOT NULL`，而 SQLite 的
  `CREATE TABLE IF NOT EXISTS` 不会改已存在的表 —— 不拦的话，旧库上的每一次提交都会
  以 code 9「内部错误」失败，信息里没有一个字与数据库有关。
- `--check` 打印**五个外部命令各自解析到的路径**（而不是配置里写了什么 ——
  配置留空是推荐值，留空之后究竟找没找到、找到的是哪一个，只有这里说得清）、
  code-server 路径**与它的来源**（本机找到的只是推测，守护进程无法在计算节点上执行
  命令，这两件事必须分开说），以及分区表与 `cluster_cidr` 交叉核对的结果。

### Fixed — 集群侧自测：CI 上那个挂了四个 commit 的失败

`cluster/test-sessiond-logic.py` 的文件头一直写着「本文件不依赖真集群，CI 上跑的与
本地一致」，**而那句话是不成立的**。两个原因：

1. `Config.validate()` 会检查 Slurm 命令**在宿主机上**是否存在，而 CI runner 上没有
   Slurm —— 于是「配置自检无错误」这一条在 CI 上必红；
2. `job_state` 的「确实不存在的作业 → MISSING」那一条用的是宿主机上的真 `scontrol`，
   在开发机上绿（本机恰好是真实集群的计算节点）、在 CI 上红（`rc=127` → UNKNOWN）。

两条红的原因都与被测逻辑毫无关系，所以看上去像「测试太严」，挂了四个 commit 没人管。
现在状态目录、作业脚本、Slurm 命令全部在 `make_config()` 里被替换成临时目录里的桩，
并且**验证过**：在一台模拟的「完全没有 Slurm」的机器上（候选目录表清空、
`which` 永远返回 None）跑出完全一样的 **168/0**。

同一节里还有两处「测的是自己抄的一份逻辑」：
`mem_ok()` 是把正则抄了一遍再测那个副本，`expand_node` 被放在桩外面靠「本机恰好装了
Slurm」通过。两处都改成调用被测代码本身。

另外补了 10 节新用例（`cluster_cidr` 的九种写法、与 `NodeAddr` 的交叉核对、旧库拒绝、
`sbatch` 命令行、`op_submit` 的 22 条决策路径），用例数 103 → 168。

### 已识别缺陷，尚未修复（集群侧，本次**未**改动）

上面修掉的是与配置有关的那三个。排查过程中还核实了另外三个，**代码一行没改**，
写在这里是为了让它们别在排查记录里丢掉 —— **不是**说已经修了：

- **`op_goodbye` 丢弃 `scancel` 的返回值。** `self.slurm.cancel(...)` 返回布尔值，
  调用点没有接（`op_goodbye`）。`scancel` 失败时（控制器抖动、munin 凭据过期、
  权限不足）会话照样走 `releasing` → `released`，**ACL 被拆掉，而作业还在集群上
  跑着**。库里那条记录说"已结束"，真相是没有 —— 这正是本项目最该避免的那类
  假绿，而且它带着安全后果：防护没了，code-server 还在监听端口。
- **`phase_release` 从不确认作业真的停了。** 它删规则、删会话文件、置 `released`，
  全程不问一句"作业还在吗"（`phase_release`）。与上一条叠加：`goodbye` 路径上
  没有任何一处会验证 `scancel` 是否奏效。修法方向是释放前用 `scontrol show job`
  确认，确认不了就停在 `releasing` 并告警，而不是宣称已结束。
- **`max_active_per_user` 数不到并发提交。** `count_active()` 只统计 `ACL_STATES`
  （`enrolled`/`suspect`/`orphaned`/`releasing`），**`submitted` 不在内**。所以这个
  上限拦不住"同时发两个 submit"—— 实际起作用的是 `max_pending_per_user`（默认 4），
  也就是每用户最多能同时挂 4 个未决提交，而不是 1 个活跃会话。
  客户端的应对是 `submit` 超时**绝不重试**、改用 `status` 认领（非幂等操作的正确
  做法），但那是绕开而不是修好。

### Added

- **客户端真实 SSH 后端从骨架变成实现**（`client/src/main/backend-ssh.js`）。
  0.1.0 的头号已知限制就是「`isImplemented()` 返回 `false`，客户端一律落到演示后端」，
  这一条现在解除：密钥认证（`privateKey`）、固定 argv 的 exec channel、
  `forwardOut` 端口转发、多路复用一条连接。
  **但它从未在真实登录节点上跑过** —— 见下方「已知限制」。
- `tools/self-test-sanitized.sh` —— 脱敏检查的反向验证，已接入 CI
- CI 新增客户端测试作业（此前 99 个用例只在开发机上手动跑过，等于没有防线）
- `client/src/main/keys.js` —— 生成并封装 OpenSSH 格式密钥。
  ssh2 **不认** Node `crypto` 导出的 PKCS#8 PEM，必须手工拼 `openssh-key-v1`；
  正确性靠密码学断言（签名 → 原生验签）而不是「能解析」
- 主机密钥 TOFU 校验（已知放行 / 首次确认 / **变更即拒绝**）
- 客户端连接条目可增删，全部在界面上 —— 此前地址只能手改 `config.json`，
  而面板上根本没有入口

## [0.1.0] - 2026-09-14

**首个公开版本。**

### Added

**集群侧（`cluster/`）**

- `slurmate-sessiond` —— root 守护进程。通过 unix socket 提供 RPC，身份取自
  `SO_PEERCRED`；管理端口池、会话状态机与孤儿回收
- `slurmate` —— 用户 CLI。`rpc` 子命令从 stdin 读一行 JSON、向 stdout 写一行 JSON，
  一次一请求；命令行是编译期常量，不接受任何用户输入拼进去
- `run.sbatch` —— 作业模板。分配端口、以 `--auth password` 启动 code-server，
  把端口与口令登记进 0600 的会话文件
- `deploy.sh` —— 一键部署。幂等、只增不改，提供 `--check` / `--dry-run` 体检模式，
  并在部署前后对 nftables 规则集做结构化比对（比对器 `nft-compare.py` 带独立自测）
- `nft-compare.py` —— nftables 规则集结构化比对器
- `slurmate.conf.example` —— 配置示例：端口区间与保留区间、配额与限流、会话续期、
  `auth_mode`、心跳阈值
- `test-sessiond-logic.py` —— 守护进程单元/集成测试，在未安装状态下用临时目录运行

**核心设计**

- **网络 ACL 的登记簿挂在 Slurm 作业上，而不是 SSH 会话上** —— 这是本项目的核心
  改动，也是它存在的理由：SSH 闪断不再丢会话
- nft 规则按作业登记的端口放行，hook 点在登录节点的 output 链
- 会话续期：剩余时间低于阈值时按增量 `scontrol update` 续期，并有累计上限
- 心跳判活与孤儿回收：停止心跳 300 秒判 `suspect`（**什么都不做**，作业与 ACL 都
  保留，等网络恢复），1800 秒判 `orphaned` 并 `scancel`
- 配额与限流：每用户活跃/未决会话上限、RPC 速率限制、拒绝计数熔断

**客户端（`client/`）**

- Electron 桌面应用。单实例锁 + 会话槽位，多开不再互相打架
- 隧道中继：本地端口按槽位持久化（端口变了 origin 就变，编辑器布局会重置），
  只监听 `127.0.0.1`
- 快捷键接管：`Ctrl+W` / `Ctrl+N` / `F5` 交给页面，`F12` / `Ctrl+Shift+I` / `F11`
  由外壳吞掉；输入法组合期间放行
- 登录节点地址表与并发探测：TCP 连上后读 SSH banner 校验，按 `priority` 取第一个
  可达的；全部不可达时逐个报出失败原因
- 登录成败按 cookie jar 判定，不看 HTTP 状态码
- 演示后端：不是一个空壳，它包含真实的本地 HTTP 服务（复刻 code-server 的登录契约）
  与真实的 TCP 隧道转发，用来复现真集群上极难复现的状态 —— 守护进程挂掉、隧道断开、
  会话被回收

**安全**

- `auth_mode` 默认 `password`（作业自生成随机口令，写进 0600 会话文件），而不是
  `none`。原因与它为什么不该被改回，见 [SECURITY.md](SECURITY.md)
- 会话文件读取做完整硬校验：拒绝符号链接、必须是 regular file、属主必须匹配、
  mode 必须为 0600、大小上限 64 KiB、`nlink == 1`
- 守护进程从不用 root 写用户文件：口令与会话文件都由作业自己生成，家目录在
  systemd 单元里以只读方式挂载

### 已知限制

- **客户端真实 SSH 后端尚未实现。** `client/src/main/backend-ssh.js` 的
  `isImplemented()` 返回 `false`，客户端因此会改用**演示后端**启动，并在界面上标注
  「演示模式 · 未连接集群」。集群侧是完整的；缺的是客户端那一跳 SSH。
- **nft ACL 拦不住同一台计算节点上的共租户。** 这是架构边界，不是缺陷，已用
  `auth_mode = password` 默认值缓解。详见 [SECURITY.md](SECURITY.md)。
- **产物未签名。** Windows SmartScreen 与 macOS Gatekeeper 会拦，Linux 的 AppImage
  需要 `chmod +x`。消除这些提示需要代码签名证书。
- **`cluster_cidr` 只能写一个 CIDR。** 登录节点与计算节点必须落在同一个网段内，
  多网段集群目前无法表达。

[0.2.0]: https://github.com/JesseFather/slurmate/releases/tag/v0.2.0
[0.1.0]: https://github.com/JesseFather/slurmate/releases/tag/v0.1.0
