# 安全策略

## 报告漏洞

**请使用 GitHub 的 Private Security Advisory，不要开公开 issue：**

<https://github.com/JesseFather/slurmate/security/advisories/new>

不要开公开 issue。集群侧的守护进程以 root 运行、客户端本地保存着登录凭据 ——
在修复发布之前把利用方式公开，等于给所有人发一份现成的攻击指南，这个窗口期不是
可以接受的风险。

报告里请尽量写清：

- 受影响的组件（`cluster/`、`client/`，还是 `plugins/` 下的某个插件）与版本
- 复现步骤、你观察到的行为、以及你预期的行为
- 该部署的 `auth_mode`，以及计算节点上是否存在按 UID 隔离的网络策略

**当前维护的版本：** `0.5.0`（当前）与 `0.2.0`（最近一个公开版本）。

> `0.3.0` 与 `0.4.0` **从未发布、从未部署过** —— 它们的内容全部并进了 `0.5.0`。
> 从 `0.2.0` 升到 `0.5.0` 是**跨版本升级，两端必须一起升**（见
> [CHANGELOG.md](./CHANGELOG.md)）。安全问题请注明你跑的是哪一版。

★ **插件是独立项目**，可以各自来自不同来源。若漏洞在某个插件里，请一并说明它是
从哪来的（仓库里那两个、还是站点/别处分发的）。基座两端的插件加载路径是同一套，
但**基座本身不认识任何插件**。

---

## 设计层面的已知边界

**下面这条不是漏洞，是架构边界。部署之前必须知道它。**

### nft ACL 拦不住同一台计算节点上的共租户

Slurmate 用 nft 规则为每个会话放行端口，规则的 hook 点是**登录节点的 output 链** ——
也就是「登录节点自己发出的包」。

- **拦得住**：登录节点上的其他用户连你的端口。
- **拦不住**：另一个作业恰好被调度到**同一台计算节点**之后，直接去 `curl` 你的端口。
  那条流量根本不经过登录节点。

这不是某个集群的配置问题，而是这个架构的固有边界。绝大多数集群的计算节点上**没有
任何 per-user 网络隔离** —— cgroup 没有 `net_cls` / `net_prio`，主机防火墙也不按
UID 区分，而本机进程访问本机 IP 走的是 `lo`，通常被放行。

**这就是 `auth_mode` 默认是 `password` 而不是 `none` 的唯一原因。**
（这个缺省值住在 code-server 插件的清单里，不在基座里 —— 见
[plugins/code-server/README.md](plugins/code-server/README.md)。）

在 `auth_mode = none` 下，同一节点上任何一个人一个 `curl` 就完整拿到你的 IDE：
你的终端、你的文件读写权限，全部以你的身份运行。共享家目录 `0700` 之类的保护恰好
被绕过 —— 因为攻击者用的就是**你的身份**。

所以：**改回 `none` 之前，请先确认你有别的手段覆盖这个缺口**，例如计算节点上确实存在
按 UID 隔离的网络策略。若没有，`none` 不是「简化部署」，而是把你的会话公开。

---

## 其余安全设计

### 守护进程从不用 root 写用户文件

`slurmate-sessiond` 以 root 运行，但它身上没有「写用户文件」这条攻击面：

- 会话口令由**作业自己**生成，写进**作业自己创建**的 0600 会话文件
  （这是宿主的职责，与哪个插件无关；插件往会话文件加字段走
  `PLUGIN_SESSION_FIELDS`，**转义由宿主统一做**）
- 守护进程只读这些文件，从不代写
- 用户家目录在 systemd 单元里以只读方式挂载（`ReadOnlyPaths`，由 `deploy.sh` 按
  `readonly_paths` 渲染）

`readonly_paths` 留空会让生成的单元不含该行，这层保护就随之消失 —— 部署后请确认
它真的进了单元文件，而不是相信它默认生效。

### 会话文件的读取做了一整套硬校验

作业写的会话文件，对守护进程来说是**不可信输入**。读取路径上每一道校验都必须成立：

| 校验 | 不这么做会怎样 |
|---|---|
| 以 `O_NOFOLLOW` 打开 | 一个符号链接指向 `/etc/shadow`，守护进程以 root 把它读出来 |
| 用 `fstat` 而非 `stat` | 打开与检查之间文件被换掉（TOCTOU） |
| `O_NONBLOCK` 打开 + `S_ISREG` | 打开一个没有写端的 FIFO 会**永久阻塞**，守护进程当场卡死；设备节点同理。`O_NONBLOCK` 让打开不阻塞，`S_ISREG` 再把这类目标拒掉 |
| `st_uid` 必须等于会话属主 | 别人伪造一个会话文件喂给守护进程 |
| `st_mode & 0o077` 必须为 0（0600） | 口令文件对同组或所有人可读 |
| `st_size` 上限 64 KiB | 无上限读取被拿来打内存 |
| `st_nlink == 1` | 硬链接共享，属主之外还有别的入口 |

清理路径上做同样的属主与类型检查 —— 否则一个伪造的条目会让守护进程删掉不属于
本系统的文件。

### 身份只取自 `SO_PEERCRED`

守护进程判定「你是谁」的唯一依据，是 `accept()` 之后立刻
`getsockopt(SO_PEERCRED)` 取到的、**内核提供的** uid/gid —— 而且是在**读任何数据
之前**取。

**不信任任何用户态可以伪造的东西**：命令行参数、环境变量、请求体里的身份字段、
客户端自称的 uid。所以 `slurmate rpc` 的命令行里没有身份信息可伪造，协议请求体里
也没有身份字段。unix socket 本身的权限位（0666）**不承担安全职责**，安全性完全
建立在内核给出的 `SO_PEERCRED` 上 —— 改权限位不会让它更安全，只会让它连不上。

### 部署脚本只增不改

`cluster/deploy.sh` 不碰任何不属于本系统的 nft 表、服务、锁目录、**站点的 sshd 配置**
与 sudoers 条目，并且**绝不执行 `nft flush ruleset`、绝不 reload 或 restart
`nftables.service`** —— 那会清掉整台机器上所有表的规则集，包括别人的。

安装任何文件之前，先检查目标是否已存在且不属于本系统：是则中止，绝不覆盖。卸载
路径同样逐项校验归属。

★ **「不碰站点的 sshd 配置」与 sshd 插件的存在不矛盾。** 那个插件在**作业里**起一个
**用户态** sshd，配置生成在作业自己的私有临时目录（`0600`）里，用 `-f` 指定 ——
所以它**不再读** `/etc/ssh/sshd_config` 及其 `sshd_config.d`。站点的 sshd 与它的
准入白名单（`pam_access` / `access.conf`）一个字都没被动过，而这个用户态实例
**就在 Slurm 监管之下**（cgroup 落在 `job_<id>/step_batch` 里）。完整的边界说明见
[plugins/sshd/README.md](plugins/sshd/README.md)。

### 插件脚本与基座源码受同一道信任门保护

插件目录里的 `job/start.sh` 内容会被 `deploy.sh` **拼进一份 root 拥有、每一个用户
都会执行**的作业脚本。所以它们与 `cluster/` 下的文件一样，必须 **root 拥有、且
组/其他不可写** —— 否则一个普通用户改写它，就等于让他的代码以**其他用户**的身份
运行。`deploy.sh` 的 `source_is_trusted()` 对两侧是同一套判据。

---

## 不在威胁模型内

- **同一台计算节点上的其他用户** —— 见上文《nft ACL 拦不住同一台计算节点上的共租户》。
  这是已知边界，已用 `auth_mode = password` 默认值缓解，不按漏洞处理。
- **已经取得你 uid 的攻击者** —— 会话文件、客户端本地配置、本地端口都归你所有，
  拿到你 uid 的人可以直接使用它们，不需要绕过任何东西。
- **运行 `slurmate-sessiond` 的那台登录节点本身被攻陷** —— 守护进程以 root 运行，
  主机失陷则一切失陷。
- **插件代码本身是可信的。** 客户端侧的插件代码**跑在主进程里**，没有签名、没有
  钉公钥、没有逐插件同意、没有进程隔离。今天这四条还不是前提，因为唯一的路是
  「用户自己放进去的东西」；**一旦有一条从远端来的路（站点分发），它们就是前提**。
  见 [docs/KNOWN-ISSUES.md](docs/KNOWN-ISSUES.md) 的 S1 / S2。

---

## 许可证

本项目以 [Apache-2.0](LICENSE) 授权。
