# 安全策略

## 报告漏洞

**请使用 GitHub 的 Private Security Advisory，不要开公开 issue：**

<https://github.com/JesseFather/slurmate/security/advisories/new>

不要开公开 issue。集群侧的守护进程以 root 运行、客户端本地保存着登录凭据 ——
在修复发布之前把利用方式公开，等于给所有人发一份现成的攻击指南，这个窗口期不是
可以接受的风险。

报告里请尽量写清：

- 受影响的组件（`cluster/`、`client/`，还是 `plugins/` 下的某个插件）与版本
- 复现步骤、你观察到的行为、以及你预期的行为
- 该部署的 `auth_mode`，以及计算节点上是否存在按 UID 隔离的网络策略

**当前维护的版本：** `0.7`（仓库里的当前版本）与 `0.2.0`（最近一个公开版本）。

> `0.6` 与 `0.7` 都**尚未发布**（`0.6` 部署过一版，而且线上那一份比仓库旧 ——
> 见 [docs/KNOWN-ISSUES.md](./docs/KNOWN-ISSUES.md) 的 U6）；
> `0.3.0` / `0.4.0` / `0.5.0` **从未发布、从未部署过** —— 它们的内容全部并进了
> `0.6`。从 `0.2.0` 升上来是**跨版本升级，两端必须一起升**（见
> [CHANGELOG.md](./CHANGELOG.md)）。安全问题请注明你跑的是哪一版。
>
> ★ 两个版本号的**段数不同是故意的**：0.6 起框架版本是两段（`major.minor`），
> 而 `0.2.0` / `0.3.0` / `0.4.0` / `0.5.0` 是**当时的字符串**，历史条目不改写。
> 报告漏洞时按你实际跑的那个串写就行 —— 判据在
> [docs/PROTOCOL.md](./docs/PROTOCOL.md) 的〈协议版本与变更〉。

★ **插件是独立项目**，可以各自来自不同来源。若漏洞在某个插件里，请一并说明它是
从哪来的（仓库里那两个、还是站点/别处分发的）。基座两端的插件加载路径是同一套，
但**基座本身不认识任何插件**。

---

## 设计层面的已知边界

**下面这条不是漏洞，是架构边界。部署之前必须知道它。**

### nft ACL 拦不住同一台计算节点上的共租户

Slurmate 用 nft 规则为每个会话放行端口，规则的 hook 点是**登录节点的 output 链** ——
也就是「登录节点自己发出的包」。

- **拦得住**：登录节点上的其他用户连你的端口。
- **拦不住**：另一个作业恰好被调度到**同一台计算节点**之后，直接去 `curl` 你的端口。
  那条流量根本不经过登录节点。

这不是某个集群的配置问题，而是这个架构的固有边界。绝大多数集群的计算节点上**没有
任何 per-user 网络隔离** —— cgroup 没有 `net_cls` / `net_prio`，主机防火墙也不按
UID 区分，而本机进程访问本机 IP 走的是 `lo`，通常被放行。

**这就是 `auth_mode` 默认是 `password` 而不是 `none` 的唯一原因。**
（这个缺省值住在 code-server 插件的清单里，不在基座里 —— 见
[plugins/code-server/README.md](plugins/code-server/README.md)。）

在 `auth_mode = none` 下，同一节点上任何一个人一个 `curl` 就完整拿到你的 IDE：
你的终端、你的文件读写权限，全部以你的身份运行。共享家目录 `0700` 之类的保护恰好
被绕过 —— 因为攻击者用的就是**你的身份**。

所以：**改回 `none` 之前，请先确认你有别的手段覆盖这个缺口**，例如计算节点上确实存在
按 UID 隔离的网络策略。若没有，`none` 不是「简化部署」，而是把你的会话公开。

---

## 其余安全设计

### 守护进程从不用 root 写用户文件

`slurmate-sessiond` 以 root 运行，但它身上没有「写用户文件」这条攻击面：

- 会话口令由**作业自己**生成，写进**作业自己创建**的 0600 会话文件
  （这是宿主的职责，与哪个插件无关；插件往会话文件加字段走
  `PLUGIN_SESSION_FIELDS`，**转义由宿主统一做**）
- 守护进程只读这些文件，从不代写
- 用户家目录在 systemd 单元里以只读方式挂载（`ReadOnlyPaths`，由 `deploy.sh` 按
  `readonly_paths` 渲染）

`readonly_paths` 留空会让生成的单元不含该行，这层保护就随之消失 —— 部署后请确认
它真的进了单元文件，而不是相信它默认生效。

### 会话文件的读取做了一整套硬校验

作业写的会话文件，对守护进程来说是**不可信输入**。读取路径上每一道校验都必须成立：

| 校验 | 不这么做会怎样 |
|---|---|
| 以 `O_NOFOLLOW` 打开 | 一个符号链接指向 `/etc/shadow`，守护进程以 root 把它读出来 |
| 用 `fstat` 而非 `stat` | 打开与检查之间文件被换掉（TOCTOU） |
| `O_NONBLOCK` 打开 + `S_ISREG` | 打开一个没有写端的 FIFO 会**永久阻塞**，守护进程当场卡死；设备节点同理。`O_NONBLOCK` 让打开不阻塞，`S_ISREG` 再把这类目标拒掉 |
| `st_uid` 必须等于会话属主 | 别人伪造一个会话文件喂给守护进程 |
| `st_mode & 0o077` 必须为 0（0600） | 口令文件对同组或所有人可读 |
| `st_size` 上限 64 KiB | 无上限读取被拿来打内存 |
| `st_nlink == 1` | 硬链接共享，属主之外还有别的入口 |

清理路径上做同样的属主与类型检查 —— 否则一个伪造的条目会让守护进程删掉不属于
本系统的文件。

### 身份只取自 `SO_PEERCRED`

守护进程判定「你是谁」的唯一依据，是 `accept()` 之后立刻
`getsockopt(SO_PEERCRED)` 取到的、**内核提供的** uid/gid —— 而且是在**读任何数据
之前**取。

**不信任任何用户态可以伪造的东西**：命令行参数、环境变量、请求体里的身份字段、
客户端自称的 uid。所以 `slurmate rpc` 的命令行里没有身份信息可伪造，协议请求体里
也没有身份字段。unix socket 本身的权限位（0666）**不承担安全职责**，安全性完全
建立在内核给出的 `SO_PEERCRED` 上 —— 改权限位不会让它更安全，只会让它连不上。

### 部署脚本只增不改

`cluster/deploy.sh` 不碰任何不属于本系统的 nft 表、服务、锁目录、**站点的 sshd 配置**
与 sudoers 条目，并且**绝不执行 `nft flush ruleset`、绝不 reload 或 restart
`nftables.service`** —— 那会清掉整台机器上所有表的规则集，包括别人的。

安装任何文件之前，先检查目标是否已存在且不属于本系统：是则中止，绝不覆盖。卸载
路径同样逐项校验归属。

★ **「不碰站点的 sshd 配置」与 sshd 插件的存在不矛盾。** 那个插件在**作业里**起一个
**用户态** sshd，配置生成在作业自己的私有临时目录（`0600`）里，用 `-f` 指定 ——
所以它**不再读** `/etc/ssh/sshd_config` 及其 `sshd_config.d`。站点的 sshd 与它的
准入白名单（`pam_access` / `access.conf`）一个字都没被动过，而这个用户态实例
**就在 Slurm 监管之下**（cgroup 落在 `job_<id>/step_batch` 里）。完整的边界说明见
[plugins/sshd/README.md](plugins/sshd/README.md)。

### 插件脚本与基座源码受同一道信任门保护

插件包里的 `job/start.sh` 内容会被 `deploy.sh` **织进 root 拥有、由某个用户提交
后以那个用户的身份执行**的作业脚本。所以那些包与 `cluster/` 下的文件一样，必须
**root 拥有、且组/其他不可写** —— 否则一个普通用户改写它，就等于让他的代码以
**其他用户**的身份运行。

★ 判据在两侧是同一套（`deploy.sh` 的 `source_is_trusted()`），而**防的东西换了
形状**：从前防的是"源目录里任何一个文件都能被普通用户改写"，现在防的是
**"root 下载完到安装器读"之间**那个窗口（包是成品，装它的是 root）。安装器自己
还会再查一遍（`package_input_problem()`：普通文件、非符号链接、属主是调用者、
022 位为空）—— 两道门，同一个判据。

★ **从一个插件一份起，这句话对「每一份」都要成立。** 作业脚本装在
`<prefix>/share/slurmate/jobs/<ULID>.sbatch`，**每一份**都是 root:root `0644`，
而那个目录本身 `0755` root:root。两条理由：

- **0644 是给提交者的**：`sbatch` 由**目标用户**的身份读那个文件（守护进程
  `fork + setuid` 之后才 exec 它）。少一个读位，症状是 `sbatch` 报"读不到文件"，
  而那句话里**没有一个字**提到权限。
- **0755 且 root 拥有的目录**：用户必须能**列**它、不能**写**它。可写的话，用户
  可以把自己的一份塞进那个目录、或把某个 ULID 换掉 —— 而守护进程是按 ULID 拼
  路径的，换掉一份就等于让某个插件跑起别人的代码。

合成一份还是 N 份**不改变信任模型**：变的只是"根拥有的可执行内容放在几个文件里"。

---

## 站点分发的插件：四条护栏的对账

`docs/ARCHITECTURE.md` 的〈插件从哪来（分层）〉把"代码分发"标成第 3 层，并写着它
必须带齐**签名、钉公钥、逐插件同意、进程隔离**四条护栏，**否则它就是一个安静的后门**。

**v0.6 做了这件事，所以这一节就是那句话说出口的答案。** 答案不是"四条都做了"
（那是假的），而是逐条说清：做了哪一条、用什么替代了哪一条、还欠哪一条、以及欠着的
那条在**用户看到的界面上**是怎么表达的。

### 四条护栏的现状

| 护栏 | 现状 |
|---|---|
| **逐插件同意** | ✅ **做了**，而且没有例外 |
| **钉公钥** | ✅ **接上了**：用户在待同意那一栏点"同意"的那一刻钉（`pinPluginKey`），此后每一份都过 `keyVerdict`。见下 |
| **签名** | ✅ **做了**（`keygen` / `sign` / 按 id 的钉表）。★ **站点侧也验了**，见下 |
| **进程隔离** | ❌ **还欠着**。这是四条里唯一需要一次**重构**的 |

### 逐插件同意：做了，而且没有例外

每个 `(id + 版本 + 整目录摘要)` 第一次都要用户点一次同意。留在本地的台账是
`~/.slurmate/config.json` 的 `trustedPlugins`，形状
`{"<id>@<版本>": {digest, site, at}}` —— 与 `hostKeys` 同一先例：一个**我自己算出来**
的值，记在本地，下次拿它比对。

★ **不给"纯声明式插件免同意"开口子。** 两个理由，任一条都够：

1. 一个恶意 `plugin.json` 也在往你机器上放东西 —— `contributes.login` 能让客户端
   往一个 URL POST 一个口令。那是**数据**不是代码，但仍然是**站点的指令**；
2. **规则一有分支，绕过它的路就会长出来。**

★ **摘要是客户端自己从字节算出来的**，不是站点报的。站点的 `package.digest` 与
`limits` 都是**自述**，拿它们当判据等于让被告当法官（`docs/PROTOCOL.md` 的
`package` / `limits` 一节）。
★ 而客户端手里那个构件是**整个包**（`plugin_package`）：**可以被签名覆盖**，所以
**验签**这件事在客户端那一侧才做得成。
★ v0.6 时这里还有一句"比逐份取多给客户端一样东西"—— 那条路（`plugin_file` + `files`）
**在 v0.7 删掉了**，而删它的**理由正是这一段**：一份一份的字节拼不出一个能被签名的
东西，所以那条路上"这份东西是谁做的"没有任何答案。它不只是多一条代码路径，
是多一条**验签绕得过去**的路。

★ **同意发生在"下载后、暂存里验完、换入之前"。** 不是下载前 —— 那时手里还没有
摘要（摘要是从字节算出来的），台账就只能按 `(id, 版本)` 记，于是"**内容变了要重新
同意**"这条需求**直接无法实现**。也不是激活前 —— 那太晚了，`loadDir()` 里那个
`require()` 就是执行。

**代价，如实说：未同意的站点仍然能往暂存目录写有界的字节**（`max_files` /
`total_bytes` 之下，目录 `0700`，且**拒绝路径上删掉的是自己的暂存**，不是站点池里
的任何东西）。这是为了让"内容变了要重新同意"能被实现而付的价。

### 钉公钥：两个判据各就各位了

⚠️ **摘要是"一致性"判据，公钥是"认证"判据。两件事不要写在同一个句子里。**

| | 它回答的问题 | 挡得住 | 挡不住 |
|---|---|---|---|
| 摘要（一致性台账） | "和我上次同意的是**不是同一份**" | 事后偷换 | **第一次给的就是坏的** |
| 公钥 / 签名（认证） | "这是不是**我以为的那个人**做的" | 上面两条都挡 | 私钥泄露 |

★ v0.6 只有左边那一格，而它当时被写成"用等价物替代"（那个等价物**严格更弱**）。
现在右边那一格也落地了，两格各管各的：

- **表**：`~/.slurmate/pinned-keys.json`，键是 **`id`**（不是 `(id, 版本)`），
  值是签名公钥的指纹。判据是 `plugin-package.js` 的 `keyVerdict`：
  从没钉过 ⇒ 首次即信任；钉过 ⇒ 此后**必须**同一把；钉过而这一份没签名 ⇒ 拒绝。
- **私钥**属于**插件**，不属于某台机器；客户端钉住的是**那一把**，所以 §4.1 那条
  "丢了私钥只能铸新 id"是它的直接后果。

三个刻意的设计点，每一个都在挡一件具体的事：

1. **它单独一个文件，不放 `config.json`。** `loadConfig` 只认已知键，于是**旧版本**
   读一遍再存一遍就会把钉子抹掉 —— 用户降级一次，钉子全没了。而那正是 §2.5 的
   分身判定有了一个重置按钮，只是按钮拿在另一个版本手里。
2. **没有"拔钉子"的函数。** §5.4 是"首次即信任、此后只能相符"，一个正常的取消钉住
   入口会把分身判定变成一次点击。（§5.4 自己写着这条弱点**没有**本地解法。）
3. **读不动的钉子**（手改过、字段缺了）**一律拒绝**，绝不退化成"从没钉过"。这一条
   与同意台账**相反**：台账里一条残缺条目丢掉 = 重新问一次（安全的那一侧）；
   钉子丢掉 = 静默重新 TOFU（不安全的那一侧）。

★ 但要说清**传输中被改**这一层**本来就有人管**，而且管它的从来不是签名：
客户端用 SSH 主机密钥做 TOFU 校验（`config.js` 的 `hostKeys` + `backend-ssh.js`），
指纹变了就**永远拒绝**（不是警告）。那一层的答案是**主机密钥**。

★ **它现在有位子了**：调用方是站点同步那条路。① 收到一份包时先过 `keyVerdict`
（在**同意对话框出现之前**）—— 判据不过就当场失败，并把两把指纹都写进那句话里
（用户要拿它去核对）；② 用户在待同意那一栏点"同意"时 `pinPluginKey`，与写同意台账
同一个时刻。

★ **v0.6 时这一段还有下半截**：逐份取那条路**也**要判 —— 它发的是**散装字节**，
拼不出一个能被签名覆盖的构件，所以对钉过的 `id` 一律给出"没有签名"⇒ 拒绝。不判的话
"让整包那条路失败"就成了一个绕开钉子的开关。**v0.7 把那条路删掉了**，所以这个缺口
关在了**结构**里：今天没有任何一条投递方式是"证不了签名者"的。★ 而当时那句判断
（"那条路要特殊对待"）本身就是它该被删掉的证据 —— 一条**需要被排除在外**的路，
不是一个中立的备选。

### 签名：做了，以及它**不**解决什么

先说不解决的那一半 —— 把论证拆成"签名**多出来的**到底是什么"，答案才诚实：

| 场景 | 签名有用吗 |
|---|---|
| **站点被攻陷** | **无效。** 攻击者有 root 就有签名密钥 —— 除非私钥不在那台机器上（它本来也不在：私钥属于**作者**） |
| **首次连接时的中间人** | **无效。** 主机密钥 TOFU 与签名 TOFU **同样弱**（都是首次即信任） |
| **站点上有个普通用户能写插件树** | **无效，**`chown`/`chmod` 才有效 —— 而 `deploy.sh` 已经做了（装完 `chown -R root:root` + `chmod -R go-w`，且要求以 root 跑） |
| **站点只是转发者、构件是第三方写的** | **★ 唯一真实的安全增量。** 主机密钥替代不了这一个 |

那个论证原先挂着**三个前提**（单站点 / 站点即作者 / 站点上那份是 root 从源码装的），
结论是"三者同时成立时，没有签名**不降低**安全性"。

★ **前提 2 与前提 3 现在都不成立了，而且不是"将来会"—— 是已经。** 插件的分发形态
变成了"**作者**用他自己的打包器产出一个 `.splug`，发布到网站 / GitHub，**管理员
下载下来**装到服务器上"，服务器侧从头到尾**没有源码树**：

- 前提 2（站点即作者）❌ —— 构件是**转发**来的，站点可能从来没见过作者；
- 前提 3（root 从源码装的）❌ —— 装的是**下载来的一个文件**，不是从检出里装；
- 前提 1（单站点）仍然是用户的选择，但已经不再被形态保证了。

按那条判据自己的话，此时签名从"锦上添花"变成"**唯一能区分作者的东西**"。
所以它不再是可选项：**`keygen` / `sign` 已经在打包器里**，客户端按 `id` 钉住
公钥（上一节），签名者变了就**拒绝**。

★ **可操作的建议只有一条：私钥要备份**（§4.1）。丢了私钥**不能**换一把接着发 ——
老用户会拒绝，作者只能给这个插件铸一个新 id，让所有人重新同意一遍。那是诚实的
结果，不是需要兜掉的失败。

### 站点侧的验签：验了，而它回答的是**另一个问题**

安装器（`slurmate plugin install`）**验签**，而且**记着上一次是谁签的**。做法是接
**系统 openssl**（`pkeyutl -verify -rawin`），不在 Python 里手写 Ed25519 —— 给一个
以 root 跑在集群上的进程加一份自己实现的密码学，是这条路上最不该省的一步。

★ **但别把这一条读过头。** 站点验签**不**回答"这是不是作者发的"：钥匙是随包一起来
的，能改包的人也能把钥匙一起换掉。它回答的是一个**更小**的问题，而那个问题在这里
恰好有用：

| | 回答什么 | 依据 | 挡的是 |
|---|---|---|---|
| 站点**验签** | 包与它带来的那把钥匙**自洽吗** | 包本身 | 传输损坏、就地改字节 |
| 站点**记签名者** | 这一次的**钥匙**与上一次是同一把吗 | 站点自己的 `.keys.json` | 管理员被喂了一个换了钥匙的包 |
| 客户端**钉公钥** | 这一次的钥匙与**我当初同意**的那把是同一把吗 | 用户自己的 `pinned-keys.json` | 站点被整个换掉 |

第二行是**站点侧的 §2.5**，也是这一版真正多出来的东西：`PLUGINS_DIR` 是持久状态，
所以站点自己也有"上一次"。规范里唯一能被机械判出来的那个案子（同 `id` 换了钥匙
⇒ 拒绝），多一个地方判它不亏。

★ 三个方向都拦，因为**"摘掉签名"与"换一把钥匙"是同一件事的两面**：记录里明明有
签名而这一份没有，安装器同样停下来问。降级到"不用验"是这条路上最便宜的攻击。

**三条如实说的边界：**

- **没验过就绝不说验过。** 本机没有 openssl 而包带签名 ⇒ **不装**（"验不了"与
  "验过了"是两回事，把前者当后者是假绿，当"没通过"则会把一台没装 openssl 的机器
  报成"有人篡改了包"）。
- **它拦不住"第一次就是坏的"**（TOFU，与 §5.4 同一形状）。首次安装没有"上一次"
  可比 —— 那一步的防线只有 `packer inspect` 的输出与管理员的眼睛。
- **它是"停下来问"，不是"永远拒绝"。** `--replace-key <旧指纹>` 能越过它，而取值
  必须是记录里那把旧指纹（确认要有依据，不是随手加一个开关）。这一点与打包器那边
  的 `--fork` / `--adopt` 是同一条纪律：**两个方向的后果相反，工具不替你选。**

### ✅ 那条"源文件信任门"的不对称，v0.6 补上了

上面那张表里"站点上那个普通用户"那一行，成立的前提是**装出来的那棵树真的是 root
装的**。这句话在 v0.6 之前有一处缺口：

`deploy.sh` 的 `source_is_trusted()` 只检查它名单里的源文件。而那个名单
（`plugin_src_files()`）**以前只列 `plugin.json` 与 `job/start.sh`**，理由是
"插件目录里其余的都不装、也不读"。**站点分发接上之后那句话不成立了**：
`client/` 整棵子树会被发到**每一台客户端**上，并在用户的 Electron 主进程里
`require()`。于是"某个普通用户能改写它"的后果，从"没什么后果"变成了
**"他的代码在每个用户的工作站上跑"**。

**v0.6 把门扩到整个插件目录。** ★ **插件变成包之后，门的形状跟着变了**，而变的是**理由**（这一版还没发出去，两者都在同一个未发布的版本里）：
站点上的插件是一个**成品包**（`.splug`），所以名单就是"`--plugins-src` 下的每一个
`.splug`"，防的是**"root 下载完到安装器读"之间**那个窗口里包被换掉。判据
（root 拥有 + 组/其他不可写）一个字没变 —— 两个形状挡的是同一类事：以 root 的身份
安装一个别人能改的字节串。

`cluster/test-sessiond-logic.py` 第 19.13 节把那份函数**抠出来真跑**，断言
"信任门恰好列出那一个真正的包文件"，并把**符号链接冒充 `.splug`** 这一种单独钉住
（`-f` 会跟随链接，所以判据里那句 `! -L` 是承重的）。

### 进程隔离：还欠着的那一条

`client/index.js` 仍然 `require()` 进 Electron 主进程：**完整 Node 权限、没有沙箱、
能读写你的凭据库与配置文件。**

★ **在它做到之前，同意界面的措辞就是唯一的安全边界。** 所以那一屏必须把话说满：
没有进程隔离、那段代码会在你这台机器上跑。一句含糊的"是否信任此插件"会让用户以为
他在同意 A 而实际同意了 B —— 那正是这个项目最恨的那类问题换了个地方出现。

`docs/KNOWN-ISSUES.md` 的 S2 记着这件事（它此前是"四条一起欠"，现在只剩这一条）。

---

## 不在威胁模型内

- **同一台计算节点上的其他用户** —— 见上文《nft ACL 拦不住同一台计算节点上的共租户》。
  这是已知边界，已用 `auth_mode = password` 默认值缓解，不按漏洞处理。
- **已经取得你 uid 的攻击者** —— 会话文件、客户端本地配置、本地端口都归你所有，
  拿到你 uid 的人可以直接使用它们，不需要绕过任何东西。
- **运行 `slurmate-sessiond` 的那台登录节点本身被攻陷** —— 守护进程以 root 运行，
  主机失陷则一切失陷。
- **插件代码本身跑在主进程里。** 站点分发的插件代码在**用户的工作站**上以完整
  Node 权限运行，没有进程隔离（签名与钉公钥的处理见上一节）。

  ⚠️ **这一条在 v0.6 之前写的是"插件代码本身是可信的……今天这四条还不是前提，
  因为唯一的路是用户自己放进去的东西"。那句话现在不成立了** —— 从远端来的那条路
  有了。所以它从"不在威胁模型内"降级成**一条已知的、还说得出代价的边界**：
  同意闸挡在换入之前，用户点过同意之后那件事就发生了，而且是**以用户的身份**
  发生的。要真正缩小它只能靠进程隔离（S2）。

---

## 许可证

本项目以 [Apache-2.0](LICENSE) 授权。
